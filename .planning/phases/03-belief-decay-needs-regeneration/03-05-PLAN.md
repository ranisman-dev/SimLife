---
phase: 03-belief-decay-needs-regeneration
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - scripts/baseline.json
  - scripts/order-baseline.json
  - PERSON-MODEL.md
  - CLAUDE.md
autonomous: false
requirements: [DECAY-01, DECAY-02, DECAY-03, DECAY-04, DECAY-05]

must_haves:
  truths:
    - "`node scripts/verify.js` exits 0 with OVERALL: PASS and every check green"
    - "The re-blessed baselines were reviewed against an explained diff, not blindly captured"
    - "PERSON-MODEL.md and CLAUDE.md no longer claim beliefs never decay, needs never regenerate, or belonging is a permanent stub"
    - "A human confirms the mind inspector renders needs and decaying belief confidence in the browser without errors"
  artifacts:
    - path: "scripts/baseline.json"
      provides: "Re-blessed golden master reflecting the {value, tick} needs shape"
      contains: "\"tick\""
    - path: "PERSON-MODEL.md"
      provides: "Updated Beliefs, Needs and Gaps sections plus the documented decision-path boundary"
      contains: "beliefConfidence"
    - path: "CLAUDE.md"
      provides: "Corrected mind-box table rows for beliefs and needs, and corrected Known gaps paragraph"
      contains: "needValue"
  key_links:
    - from: "PERSON-MODEL.md Needs section"
      to: "sim.js needValue/adjustNeed"
      via: "documents the {value, tick} shape, the regen formula and TUNING.needRegenRate"
      pattern: "needValue"
    - from: "CLAUDE.md mind-box table"
      to: "sim.js belief decay + needs regen"
      via: "Decay? column for beliefs and needs rows"
      pattern: "needs"
---

<objective>
Close the phase: confirm every check is green under the final tuning, perform the single
human-reviewed golden-master re-bless, bring the two documentation files back in sync with
the code, and have a human confirm the browser UI actually works.

Purpose: Plans 03-01 through 03-04 were each forbidden from running `--update-baseline`
because the needs-regeneration rate is coupled to Phase 2's LOCKED `ORDER_SPEC` fixture and
could only be judged once D-04 and D-07 were both live (that verification happened in Plan
03-04 Task 3). This plan is the one sanctioned re-bless. `CLAUDE.md` also treats drift
between `PERSON-MODEL.md` and `sim.js` as a bug in one or the other, and this phase
falsifies four specific documented claims — fixing them is a phase deliverable, not
housekeeping.

Output: re-blessed `scripts/baseline.json` and `scripts/order-baseline.json`, updated
`PERSON-MODEL.md` and `CLAUDE.md`, and a human browser verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@PERSON-MODEL.md
@.planning/phases/03-belief-decay-needs-regeneration/03-CONTEXT.md
@.planning/phases/03-belief-decay-needs-regeneration/03-PATTERNS.md
@.planning/phases/03-belief-decay-needs-regeneration/03-01-SUMMARY.md
@.planning/phases/03-belief-decay-needs-regeneration/03-02-SUMMARY.md
@.planning/phases/03-belief-decay-needs-regeneration/03-03-SUMMARY.md
@.planning/phases/03-belief-decay-needs-regeneration/03-04-SUMMARY.md

**Re-bless mechanics (from `scripts/verify.js`, read in full during pattern mapping):**

- `node scripts/verify.js --update-baseline` REFUSES (exit 1, `verify.js:237-245`) while any
  *qualitative* check is failing unacknowledged. The two golden-master checks
  (`snapshot-matches-baseline`, `order-matches-baseline`) are exempt from that refusal gate
  (`verify.js:41`, `231-232`) precisely so a legitimate structural change like this phase's
  can be blessed at all — and neither can ever be silenced via `known-mismatch.json`.
- The flag writes BOTH `scripts/baseline.json` and `scripts/order-baseline.json`
  (`verify.js:248-249`). It must NOT write `scripts/order-prefix.json`, which is Phase 2's
  frozen pre-fix "before" side and is only ever produced by `--capture-prefix-order`.
- `scripts/known-mismatch.json` does not exist and must not be created by this plan.

**Documentation drift this phase created — exact anchors:**

- `CLAUDE.md:70` — beliefs row: `| ... | confidence can be revised | never — array only grows |`
- `CLAUDE.md:72` — needs row: `| ... | only drops, two triggers total | never regenerates |`
- `CLAUDE.md:107-108` — Known gaps paragraph: "beliefs never decay or get pruned; needs never
  regenerate; `belonging` has no triggers at all"
- `PERSON-MODEL.md:180` — "**Beliefs never decay and are never pruned.** The array only grows."
- `PERSON-MODEL.md:213-219` — Needs section: `{ safety, sustenance, belonging }`, default
  `{1, 1, 0.6}`, "**`belonging` is wired to nothing anywhere — pure stub, permanently**"
- `PERSON-MODEL.md:362-370` — "Pre-existing stubs" / "Needs regeneration and `belonging`. No
  need currently recovers on its own, and `belonging` has no triggers at all."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite confirmation and the single human-reviewed golden-master re-bless</name>

  <read_first>
    - `scripts/verify.js:106-160` — `main()`'s check batches, both diff prints, and the section headers
    - `scripts/verify.js:220-255` — the refusal gate, `NON_ACKNOWLEDGEABLE_CHECK_NAMES`, and both baseline writes
    - `.planning/phases/03-belief-decay-needs-regeneration/03-04-SUMMARY.md` — the final `TUNING.needRegenRate` and the recorded ORDER-01 verification
    - `.planning/phases/02-witness-reaction-ordering/02-03-SUMMARY.md` — the previous re-bless diff and the eleven-check green output, for comparison
  </read_first>

  <files>scripts/baseline.json, scripts/order-baseline.json</files>

  <action>
Run `node scripts/verify.js` and capture the complete output verbatim.

Before blessing anything, confirm and record that every qualitative check is green: the five
Phase 1 checks (`clone-specs-differ-in-exactly-one-field`, `drift-disabled`,
`positive-clone-attacks-player`, `negative-clone-takes-no-action`, `reactions-diverge`), the
four ORDER-01 checks (`dispatch-order-differs-from-agent-list`, `victim-dispatched-first`,
`victim-retaliates-first`, `indifferent-witness-dispatched-last`), and all thirteen DECAY
checks. The only permitted red lines are `snapshot-matches-baseline` and
`order-matches-baseline`.

Review the printed `Baseline diff:` block against the four mechanisms `03-PATTERNS.md`
predicted, and classify every diff entry as expected-and-explained before blessing:
  1. `...needs.<key>` removed with `...needs.<key>.value` / `...needs.<key>.tick` added, for
     every serialized agent in both the `jungle` and `averse` snapshots — D-04's shape change.
  2. Any `...beliefs.<index>` content or length change — D-03's pruning.
  3. Any `witnessOrder` / reaction change — must be explained against 02-03-SUMMARY.md's
     recorded post-fix order, since Plan 03-04 Task 3 verified the ORDER-01 checks still hold.
  4. Any `belonging` value change — D-05's new triggers firing in a scenario that performs a
     `Give` or an `is_trustworthy` `Tell`.
If any diff entry fits none of those four categories, STOP and report it rather than
blessing: an unexplained entry is exactly what the golden master exists to catch.

Then run `node scripts/verify.js --update-baseline` once, and re-run plain
`node scripts/verify.js` to confirm it now exits 0 with `OVERALL: PASS`.

Confirm `git status --short` shows `scripts/baseline.json` and `scripts/order-baseline.json`
modified and `scripts/order-prefix.json` UNCHANGED — Phase 2's frozen pre-fix comparison must
survive this phase intact, exactly as Plan 02-03 preserved it. Also confirm the permanent
`Ordering fix effect (pre-fix -> current)` section still prints on every run.

Do not create `scripts/known-mismatch.json`. Do not modify `sim.js` in this plan at all —
the regeneration rate and its in-code comment were settled, verified against `ORDER_SPEC`,
and recorded in Plan 03-04 Task 3, which owns that constant. This plan owns the baselines
and the two documentation files only. If the rate looks wrong here, that is a stop-and-report
condition, not an edit.
  </action>

  <verify>
    <automated>node scripts/verify.js > /dev/null 2>&1; echo "exit=$?"; node scripts/verify.js | tail -1</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/verify.js` exits 0 and its last line is exactly `OVERALL: PASS`
    - The full pre-bless output, including the complete `Baseline diff:` block, is recorded verbatim in the summary with every entry classified into one of the four predicted mechanisms
    - `node -e "const S=require('./sim.js');const a=[].concat(S.runRegressionCheck({}).checks,S.runOrderingCheck({}).checks,S.runDecayCheck().checks);const bad=a.filter(c=>!c.pass);console.log(bad.length===0?'ALL GREEN '+a.length:'RED: '+bad.map(c=>c.name).join(','));"` prints `ALL GREEN` with a count of at least 22
    - `git status --short scripts/` shows `scripts/baseline.json` and `scripts/order-baseline.json` modified; `git diff --stat scripts/order-prefix.json` is empty
    - `ls scripts/known-mismatch.json` fails (the file was never created)
    - `grep -c '"tick"' scripts/baseline.json` is greater than 0 (the new needs record shape is actually captured)
    - The `Ordering fix effect (pre-fix (computeWitnesses agent-list dispatch order) -> current):` section still prints in the plain run
  </acceptance_criteria>

  <done>Every check is green, the two baselines are re-blessed after a diff review that classified every entry, `order-prefix.json` is untouched, and no check was acknowledged away.</done>
</task>

<task type="auto">
  <name>Task 2: Doc sync — PERSON-MODEL.md and CLAUDE.md brought back in step with sim.js</name>

  <read_first>
    - `PERSON-MODEL.md:145-225` — the Beliefs, Memories and Needs sections (the stale claims at 180 and 213-219)
    - `PERSON-MODEL.md:325-372` — the Gaps sections, including "Needs regeneration and `belonging`" at 369-370
    - `CLAUDE.md:60-110` — the mind-box table (rows at 70 and 72) and the Known gaps paragraph (107-108)
    - `sim.js:40-60` — the final `TUNING` block with all six new constants and their comments
    - `sim.js` — `beliefConfidence`, `needValue`, `adjustNeed`, `isCurrentlyRetreating`, and the two `belonging` hooks, so the docs describe what was actually built
    - All four prior summaries in this phase, for decisions and deviations that belong in the docs
  </read_first>

  <files>PERSON-MODEL.md, CLAUDE.md</files>

  <action>
Update `PERSON-MODEL.md` so no claim contradicts `sim.js`. Required edits:

  - **Beliefs section (~line 180):** replace "**Beliefs never decay and are never pruned.**
    The array only grows." with the real behavior: effective confidence is computed live by
    `beliefConfidence(belief, currentTick)` using the same halflife shape as
    `memoryStrength` (`halflife = 3 + confidence * 35`), and stale beliefs are pruned at push
    time (both push sites) below `TUNING.beliefPruneFloor`. State the D-02 exemption
    explicitly: a belief whose `source` contains `known false` is never pruned at any age,
    because a contradicted belief is the most protective record in the mind, not the least.
    State that beliefs have no length cap and why one would violate that exemption.
  - **The decision-path boundary, stated deliberately** (this is the phase's one documented
    limit, and a reader must be able to tell it is a choice rather than an oversight):
    decayed confidence is consumed by pruning and by display; decision-path reads —
    `findConflictingBeliefs`' `confidence <= 0.2` eligibility gate, `existingSupport`, and
    `believesDead`'s `confidence > 0.4` gate — still read stored confidence. Decay reaches
    those gates by the record disappearing, mirroring `memoryStrengthForEvent`'s own
    `// no memory record left = genuinely forgotten` contract at `sim.js:503`. Note that
    rewiring `believesDead` to live confidence would make an NPC forget a death after roughly
    50 ticks, which no requirement asks for.
  - **Needs section (~213-219):** document the `{ value, tick }` record shape (replacing the
    flat-number description), `NEED_DEFAULTS` as the single source of per-key defaults
    (`belonging` still starts at `0.6`), the live accessor `needValue(agent, needKey, currentTick)`
    with its asymptotic approach toward `1` at `TUNING.needRegenRate` per elapsed tick, and
    the fact that `adjustNeed(agent, needName, delta, tick)` regenerates to `tick` before
    applying its delta and then re-stamps. Delete the "**`belonging` is wired to nothing
    anywhere — pure stub, permanently**" claim and replace it with the two real triggers: a
    `Give` raises the giver's own belonging by `TUNING.belongingGiveGain`, and an
    `is_trustworthy` `Tell` raises the teller's by `TUNING.belongingVouchGain`, both guarded
    on `!actor.isPlayer` because the player has `mind: null` — noting that this guard is
    inverted relative to the `Take`/`Attack` hooks because these raise the actor's need rather
    than lowering the target's.
  - **Decision provenance / Phase 2 section (~289):** add the retreat hysteresis: the gate is
    now a two-threshold band (`TUNING.retreatSafetyEnter` 0.65 / `TUNING.retreatSafetyExit`
    0.75), and "currently retreating" is derived by `isCurrentlyRetreating(agent)` scanning
    `mind.log` backward for the most recent entry whose `considered` array carries a
    `retreat=` term and checking whether that entry chose `retreat`. State why the mechanism
    is a log scan rather than a new `mind` field: `scoreCandidates` is called twice per
    witness per event (the `orderWitnesses` pre-pass and real dispatch) and must stay pure, so
    no new mutable state was added to the `mind` box.
  - **Gaps section (~362-372):** remove "Needs regeneration and `belonging`" from the
    pre-existing stubs list — it is now shipped. Add the remaining honest gaps this phase did
    not close: decayed confidence is not consumed by decision-path gates (the boundary above),
    and `mind.log` still grows unbounded (untouched by this phase; the log-scan hysteresis
    reads only the tail, so it neither introduces nor worsens that).

Update `CLAUDE.md`:
  - **Line 70, beliefs row Decay? column:** replace "never — array only grows" with the real
    behavior — computed live via `beliefConfidence`, pruned on push below
    `TUNING.beliefPruneFloor`, `known false` beliefs exempt.
  - **Line 72, needs row:** the Mutable? column becomes the real trigger set (drops on
    `Take`/`Attack`, rises on `Give`/`is_trustworthy` `Tell`), and the Decay? column becomes
    regeneration computed live via `needValue`. Keep the table's existing terse style and
    column structure.
  - **Lines 107-108, Known gaps paragraph:** remove "beliefs never decay or get pruned; needs
    never regenerate; `belonging` has no triggers at all". Leave the remaining headline gaps
    (sticky-not-static drift, `ReplenishFood`) intact and correct.

Keep both documents in their existing voice and structure. Do not restructure sections, do not
add a changelog, and do not describe anything that was not actually built — read the four
summaries for what shipped, including any recorded deviation.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const pm=fs.readFileSync('PERSON-MODEL.md','utf8');const cm=fs.readFileSync('CLAUDE.md','utf8');const stale=[['PERSON-MODEL.md','Beliefs never decay',/Beliefs never decay/],['PERSON-MODEL.md','belonging permanent stub',/wired to nothing anywhere/],['CLAUDE.md','never regenerates',/never regenerates/],['CLAUDE.md','array only grows',/array only grows/],['CLAUDE.md','beliefs never decay',/beliefs never decay/]];const found=stale.filter(([f,l,re])=>re.test(f==='CLAUDE.md'?cm:pm));if(found.length)throw new Error('stale claims remain: '+found.map(x=>x[0]+': '+x[1]).join(' | '));for(const t of ['beliefConfidence','needValue','known false'])if(!pm.includes(t))throw new Error('PERSON-MODEL.md missing '+t);for(const t of ['needValue','beliefConfidence'])if(!cm.includes(t))throw new Error('CLAUDE.md missing '+t);console.log('OK');"</automated>
  </verify>

  <acceptance_criteria>
    - The automated command prints `OK` and exits 0 (all five stale claims gone, new mechanisms named in both files)
    - `grep -n 'belonging' PERSON-MODEL.md` shows the two real triggers documented and no "pure stub" / "wired to nothing" language anywhere
    - `grep -n 'isCurrentlyRetreating\|retreatSafetyEnter' PERSON-MODEL.md` returns at least one hit documenting the hysteresis band and its mechanism
    - `PERSON-MODEL.md` contains the decision-path boundary paragraph naming `findConflictingBeliefs`, `believesDead`, and `sim.js:503`'s "genuinely forgotten" contract
    - Every `TUNING` constant added in this phase (`beliefPruneFloor`, `needRegenRate`, `belongingGiveGain`, `belongingVouchGain`, `retreatSafetyEnter`, `retreatSafetyExit`) is named in `PERSON-MODEL.md`
    - `CLAUDE.md`'s mind-box table still has exactly its original column structure and row count (`grep -c '^| ' CLAUDE.md` unchanged from before this task)
    - `node scripts/verify.js` still exits 0 with `OVERALL: PASS` (docs-only change, no behavior touched)
    - `git status --short` shows only `PERSON-MODEL.md` and `CLAUDE.md` modified by this task
  </acceptance_criteria>

  <done>Neither document makes a claim `sim.js` contradicts, the new mechanisms and constants are documented, and the one deliberate scope boundary is recorded as a choice rather than left as apparent drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human browser verification of the mind inspector and debug report</name>

  <read_first>
    - `presentation.js:60-125` — `renderMind`, the needs bar-list and belief list as rewired in Plan 03-02
    - `presentation.js:158-190` — `buildDebugReport`'s needs and belief lines
    - `index.html` — the mind-panel and debug-report controls the human will click
    - `.planning/phases/02-witness-reaction-ordering/02-04-HUMAN-UAT.md` — the persisted-checkpoint precedent to follow if the human is unavailable this session
  </read_first>

  <what-built>
    Beliefs now decay and self-prune (keeping `known false` records forever); all three needs
    regenerate toward 1 through a live accessor; `belonging` has its first two triggers; the
    retreat gate has a hysteresis band; and `presentation.js`'s four read sites were rewired
    so the mind inspector and debug report survive the `mind.needs` shape change instead of
    throwing `TypeError: v.toFixed is not a function`.

    Everything automatable has been verified: 22+ checks green via `node scripts/verify.js`,
    both baselines re-blessed after a reviewed diff, and both docs re-synced. What no
    automated check in this repo can cover is the actual DOM render — there is no headless
    browser here, and the needs/belief render paths are exactly what this phase restructured.
  </what-built>

  <how-to-verify>
    1. Start the static server from the repo root: `python3 -m http.server 8000`
    2. Open `http://localhost:8000/index.html` and open the browser devtools Console.
    3. Click an NPC to open the mind inspector. Confirm the **Needs** section renders three
       bars (safety, sustenance, belonging) with numeric values between 0.00 and 1.00 and
       visible bar fills — no blank list, no `NaN`, no `[object Object]`.
    4. Confirm the Console shows NO `TypeError: v.toFixed is not a function` and no other red
       errors.
    5. Type `attack <npc name>` in the command input and run it. Reopen that NPC's inspector
       and confirm their **safety** bar has visibly dropped.
    6. Perform several more unrelated actions (any verbs), then reopen the same NPC and
       confirm **safety** has visibly risen back toward 1 without ever reaching a value above
       1.00 — this is DECAY-03 observed by eye.
    7. Type `give bread to <npc name>` as the player. Confirm nothing throws in the Console
       (this is the inverted `!actor.isPlayer` guard on the live path — the most likely way
       this phase breaks the running game).
    8. Have an NPC's belief list open and perform several more actions. Confirm the belief
       percentages **decrease** as ticks pass rather than staying frozen — this is DECAY-01
       observed by eye. Old, low-confidence beliefs should eventually drop off the list.
    9. Click the debug-report button and confirm the report builds and contains a `needs:`
       line with three numeric values and belief lines showing live-vs-stored confidence.

    Report anything unexpected verbatim, including the full Console error text if any appears.
  </how-to-verify>

  <resume-signal>Type "approved" if steps 3-9 all behave as described, or describe exactly what differed (with verbatim Console output for any error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/verify.js --update-baseline` → `scripts/*.json` | The one point in the phase where the regression gate's reference state is rewritten. |
| `sim.js` engine state → `presentation.js` DOM render | Verified by a human in this plan because no headless browser exists in the repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Baseline re-blessed while hiding an unexplained regression | mitigate | Task 1 requires every diff entry be classified into one of four predicted mechanisms before blessing, and stop-and-report for anything unclassifiable; `verify.js`'s own refusal gate independently blocks blessing while any qualitative check is red. |
| T-03-11 | Repudiation | `scripts/order-prefix.json` overwritten, destroying Phase 2's frozen ORDER-02 "before" evidence | mitigate | Acceptance criterion asserts `git diff --stat scripts/order-prefix.json` is empty; `--update-baseline` does not write that path. |
| T-03-12 | Information Disclosure | Debug report pasted publicly | accept | Contains only simulated NPC state from a local prototype; no user data, credentials, or PII exist in this project. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero-dependency static site, no package.json, no install task in this phase. |
</threat_model>

<verification>
- `node scripts/verify.js` exits 0, last line `OVERALL: PASS`, 22+ checks green
- `scripts/order-prefix.json` unchanged; `scripts/known-mismatch.json` absent
- No stale documentation claim survives in either doc (automated string check)
- Human confirms browser render, need drop/recovery, player Give, and visible belief decay
</verification>

<success_criteria>
- DECAY-01 through DECAY-05 all verified green in one run under one final tuning
- The single golden-master re-bless happened after a classified diff review
- `PERSON-MODEL.md` and `CLAUDE.md` match `sim.js`, with the one deliberate scope boundary documented as a choice
- A human has confirmed the UI works, closing the gap no check in this repo can cover
</success_criteria>

<output>
Create `.planning/phases/03-belief-decay-needs-regeneration/03-05-SUMMARY.md` when done.
Record verbatim: the pre-bless `node scripts/verify.js` output with the full classified
`Baseline diff:` block, the post-bless output showing `OVERALL: PASS`, the final
`TUNING` values for all six phase constants, and the human's verbatim checkpoint response.
If the checkpoint is not reached in this session, persist it as
`03-05-HUMAN-UAT.md` following Phase 2's `02-04-HUMAN-UAT.md` precedent.
</output>
