---
phase: 02-witness-reaction-ordering
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - CLAUDE.md
  - PERSON-MODEL.md
autonomous: false
requirements: [ORDER-01, ORDER-02]

must_haves:
  truths:
    - "CLAUDE.md's action-pipeline description names the urgency-ordering step, so a future agent reading it does not assume agent-list dispatch."
    - "PERSON-MODEL.md describes the extracted pure scorer, the deferred gossip roll, and the ordering pass, matching what sim.js actually does."
    - "A human has confirmed in a real browser that reordered dispatch reads correctly in the event log and mind inspector."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected action-pipeline flow and decide/act description; corrected verification-command sentence"
      contains: "orderWitnesses"
    - path: "PERSON-MODEL.md"
      provides: "Witness reaction ordering + scoring-purity documentation"
      contains: "witnessOrder"
  key_links:
    - from: "CLAUDE.md action pipeline"
      to: "sim.js orderWitnesses"
      via: "named step in the documented flow"
      pattern: "orderWitnesses"
    - from: "PERSON-MODEL.md"
      to: "sim.js scoreCandidates"
      via: "named function in the decision-provenance section"
      pattern: "scoreCandidates"
---

<objective>
Close this phase's doc-sync obligation and get a human's eyes on the reordered behavior in a
real browser.

Purpose: `CLAUDE.md` states as a project rule that drift between the docs and `sim.js` is a bug
in one or the other. Plans 02-01 through 02-03 changed the dispatch flow, added a pure scorer,
moved two RNG draws, and added a new event field — every one of those makes a sentence in
`CLAUDE.md` or `PERSON-MODEL.md` false. This plan makes them true again, then verifies the
change reads correctly to a person rather than only to a diff.

Output: updated `CLAUDE.md` and `PERSON-MODEL.md`; a human-confirmed browser check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@PERSON-MODEL.md
@.planning/phases/02-witness-reaction-ordering/02-CONTEXT.md
@.planning/phases/02-witness-reaction-ordering/02-01-SUMMARY.md
@.planning/phases/02-witness-reaction-ordering/02-02-SUMMARY.md
@.planning/phases/02-witness-reaction-ordering/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring CLAUDE.md and PERSON-MODEL.md back in sync with sim.js</name>
  <files>CLAUDE.md, PERSON-MODEL.md</files>
  <read_first>
    - `CLAUDE.md` in full — specifically §"Running it", §"Core architecture (sim.js)" (the **Action pipeline** and **Decide/act** paragraphs), and the mind-box table
    - `PERSON-MODEL.md` lines 145-200 (the Beliefs section's note that `computeWitnesses()` excludes the actor) and lines 270-345 (§"Decision provenance", §"Gaps for the next phase", §"Pre-existing stubs")
    - `sim.js` — `orderWitnesses`, `computeWitnesses`, `performAction`, `scoreCandidates`, `decideAndAct` as they now stand
    - `.planning/phases/01-verification-infrastructure/01-03-SUMMARY.md` §"Follow-ups (out of scope, not fixed)" — items (a), (b), (c) and their precise replacement directions
  </read_first>
  <action>
    Four edits, all narrow. Do not restructure either document.

    **CLAUDE.md, §"Core architecture (sim.js)", the Action pipeline paragraph.** The sentence
    currently reads: `Flow: checkPreconditions → applyEffects → push an event onto world.events
    → computeWitnesses → perceiveEvent for each witness.` It is now false — dispatch is no
    longer in `computeWitnesses` order. Rewrite the flow to name the new step:
    `checkPreconditions` → `applyEffects` → push an `event` onto `world.events` →
    `computeWitnesses` → `orderWitnesses` → `perceiveEvent` for each witness, highest urgency
    first. Add one sentence stating that `orderWitnesses` scores every witness's candidate
    reactions with the pure `scoreCandidates()` and sorts descending, that witnesses whose
    appraisal falls in `decideAndAct`'s no-reaction band sort last, that ties fall back to
    `agentsAt` list order via a stable sort with no randomness involved, and that the resulting
    sequence is recorded on the event as `event.witnessOrder` for inspection. Keep the existing
    sentence about `MAX_REACTION_DEPTH` and the `reactionDepth` counter exactly as it is — that
    machinery is unchanged and the file's warning about its fragility still applies.

    **CLAUDE.md, §"Core architecture (sim.js)", the Decide/act paragraph.** It currently says
    `decideAndAct()` "scores candidate reactions ... and takes the highest-scoring one".
    Update it to say that the scoring itself now lives in a separate pure `scoreCandidates()`
    (no mutation, no RNG draws, safe to call more than once), which both `decideAndAct()` and
    `orderWitnesses()` call, and that `decideAndAct()` picks the top candidate, resolves it, and
    fires it. Add that the "tell a confidant" candidate's honesty flip and scapegoat pick are
    deferred into a `resolve()` closure that runs only for the winning candidate, so merely
    considering gossip no longer consumes randomness.

    **CLAUDE.md, §"Running it".** The sentence `There is no test suite, linter, or build command
    in this repo.` has been false since Phase 1 shipped `scripts/verify.js` (01-03-SUMMARY
    follow-up (a)). Narrow it rather than deleting it: there is still no linter, no build step,
    and no test suite in the conventional sense, but `node scripts/verify.js` is a committed,
    repeatable regression check with a `--update-baseline` golden-master workflow, and as of this
    phase it also runs the witness-ordering checks and prints the pre-fix → current ordering
    diff. Mention `--capture-prefix-order` only if the surrounding paragraph already lists flags;
    otherwise leave it to `scripts/verify.js` itself.

    **PERSON-MODEL.md, §"Decision provenance (`event.why`, `mind.log[].why`)".** Append a short
    subsection (roughly one screen, matching the file's existing prose density) covering, in
    this order:
      - Scoring is now a pure function, `scoreCandidates(world, witness, event, appraisal,
        priorRelationship)`, returning `{ reacts, candidates }` with `candidates` pre-sorted
        descending by score and never empty. `reacts` is false exactly when
        `appraisal.impact >= -0.05` — the band where a witness registers the event but does not
        care enough to act — and in that case the list holds only the `do nothing` candidate.
      - The named-`terms` + `Object.values(...).reduce` idiom, `explainTerms()`, `event.why` and
        `mind.log[].why` are all unchanged; the extraction relocated the code, not the maths.
      - Witnesses to a shared event are dispatched in descending top-candidate-score order, not
        in `world.agents` insertion order. Witnesses in the no-reaction band sort last, ties keep
        `agentsAt` order via a stable sort, and no randomness is involved anywhere in ordering.
        The dispatched sequence is recorded on the event as `event.witnessOrder`, a
        display/inspection field in the same family as `causedBy` and `why`, never read back
        into scoring.
      - Ordering is a two-pass design: scores are computed for all witnesses first, then each
        witness's `perceiveEvent` cascade runs in that order and *recomputes* its own appraisal
        and candidate scores fresh. The pre-pass number decides only position in the queue. The
        visible consequence, which is accepted and not a bug: a witness can be sorted first and
        then take no action, because an earlier witness's cascade moved their appraisal into the
        no-reaction band before their turn came.
      - The "tell a confidant" candidate's truth/lie flip and `pickScapegoat` weighted draw now
        happen only when that candidate wins, inside a `resolve()` closure. Consequence for the
        mind inspector: the `considered` list shows the gossip candidate without the
        `(misattributed)` marker, since misattribution is not decided until the candidate is
        chosen; the `chose` label still carries it, and the misattribution is visible on the
        resulting `Tell` event as `data.claim.subject` naming someone other than the real actor.

    Do **not** touch `.planning/codebase/CONCERNS.md`, `.planning/codebase/CONVENTIONS.md`, or
    any other file under `.planning/codebase/` — those are generated by `/gsd:map-codebase` and
    the standing precedent (01-03-SUMMARY follow-up (c)) is that they are corrected by
    regeneration, never by hand.
  </action>
  <acceptance_criteria>
    - `grep -c 'orderWitnesses' CLAUDE.md` is at least `1`.
    - `grep -c 'scoreCandidates' CLAUDE.md` is at least `1`.
    - `grep -c 'computeWitnesses. → .perceiveEvent' CLAUDE.md` outputs `0` — the stale direct adjacency (`computeWitnesses` → `perceiveEvent`, with the backticks matched by the `.` wildcards) is gone from the documented flow.
    - `grep -c 'orderWitnesses. → .perceiveEvent' CLAUDE.md` is at least `1` — the new step sits between `computeWitnesses` and `perceiveEvent` in the documented flow.
    - `grep -c 'There is no test suite, linter, or build command in this repo' CLAUDE.md` outputs `0`.
    - `grep -c 'scripts/verify.js' CLAUDE.md` is at least `1`.
    - `grep -c 'witnessOrder' PERSON-MODEL.md` is at least `1`; `grep -c 'scoreCandidates' PERSON-MODEL.md` is at least `1`; `grep -c 'resolve()' PERSON-MODEL.md` is at least `1`.
    - `git status --short -- .planning/codebase/` produces no output — no generated map file was hand-edited.
    - `git show --stat HEAD` for this task's commit lists exactly `CLAUDE.md` and `PERSON-MODEL.md`.
    - `node scripts/verify.js` still exits 0 (documentation edits must not disturb the engine).
    - Every claim added to either document is checkable against `sim.js`: for each of `orderWitnesses`, `scoreCandidates`, `witnessOrder`, and `resolve`, `grep -c '<token>' sim.js` is at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify.js && node -e "const fs=require('fs');const c=fs.readFileSync('CLAUDE.md','utf8');const p=fs.readFileSync('PERSON-MODEL.md','utf8');const s=fs.readFileSync('sim.js','utf8');const ok=c.includes('orderWitnesses')&&c.includes('scoreCandidates')&&!c.includes('There is no test suite, linter, or build command in this repo')&&p.includes('witnessOrder')&&p.includes('scoreCandidates')&&['orderWitnesses','scoreCandidates','witnessOrder','resolve'].every(t=>s.includes(t));console.log('doc sync:',ok?'OK':'INCOMPLETE');process.exit(ok?0:1)"</automated>
  </verify>
  <done>`CLAUDE.md`'s pipeline and decide/act descriptions and its verification-command sentence match reality; `PERSON-MODEL.md` documents the pure scorer, the ordering pass, `event.witnessOrder`, and the deferred gossip roll; no generated codebase map was hand-edited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification of reordered witness dispatch</name>
  <files>none</files>
  <read_first>
    - `.planning/phases/02-witness-reaction-ordering/02-03-SUMMARY.md` (the verbatim ordering diff, so the checkpoint prompt can quote the expected order)
    - `presentation.js` lines 30-50 (event log rendering), 100-115 (mind-inspector `considered`/`chose`/`why` rendering), 140-205 (the debug report)
    - `.planning/phases/01-verification-infrastructure/01-03-SUMMARY.md` §"Issues Encountered" (on this machine `python3` resolves to a Microsoft Store stub — use `python -m http.server 8000`)
  </read_first>
  <what-built>
    Plans 02-01 through 02-04 replaced `performAction`'s agent-list witness dispatch with an
    urgency-ordered pass, extracted a pure `scoreCandidates()`, deferred the gossip RNG draws to
    the winning candidate only, added `event.witnessOrder` provenance, and brought `CLAUDE.md`
    and `PERSON-MODEL.md` back in sync. `node scripts/verify.js` is green, including four
    ordering checks and the printed pre-fix → current ordering diff.

    Before handing off, start the static server yourself and confirm it responds — do not ask
    the human to start it. Use `python -m http.server 8000` from the repo root (`python3` is a
    Microsoft Store alias stub on this machine and serves nothing), confirm
    `http://localhost:8000/index.html` returns HTTP 200, and only then present the steps below.
  </what-built>
  <how-to-verify>
    1. Open `http://localhost:8000/index.html`.
    2. In the dev console, run `Sim.runOrderingCheck({})` and confirm it returns `pass: true`
       with four checks, all `pass: true`.
    3. In the dev console, run:
       `const w = Sim.buildOrderingScenario(); console.log(w.world.events.find(e => e.id === w.eventId).witnessOrder.join(', '))`
       and confirm it prints `garrick, elena, ...` with `ives` last — the victim first, the
       indifferent bystander last. Report the exact string you see.
    4. Back in the actual game UI, type `attack garrick` and press enter. In the event log,
       confirm that Garrick's own reaction (`↳ reaction to #<n>`) appears *before* any other
       NPC's reaction to that same event, and that each reaction still shows its
       `— due to <terms>` explanation.
    5. Open the mind inspector for two or three NPCs. Confirm each shows exactly one decision
       entry per event (no duplicated `considered`/`chose` rows), that the `considered` lists
       still read sensibly, and that any `tell ... about ...` entry in a `considered` list has
       no `(misattributed)` suffix while a `chose:` line may still have one. Report anything
       that reads as garbled, duplicated, or empty.
    6. Click the reset button and confirm the app re-initializes without a console error
       originating from `sim.js` or `presentation.js`. (A `"A listener indicated an
       asynchronous response..."` message tracing to `index.html:1` is a known browser-extension
       artifact, not this codebase — Phase 1 hit the same one.)
  </how-to-verify>
  <acceptance_criteria>
    - The human reports the verbatim `witnessOrder` string from step 3, and it begins with `garrick` and ends with `ives`.
    - The human confirms Garrick's reaction precedes every other NPC's reaction to the same event in the live event log (step 4).
    - The human confirms no duplicated decision entries in any inspected mind (step 5).
    - The human confirms no console error tracing to `sim.js` or `presentation.js` (step 6).
    - The disposition — `approved`, or the specific issues reported — is recorded verbatim in the SUMMARY, along with any code change made in response.
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe exactly what looked wrong (which step, what you saw, what you expected).</resume-signal>
  <verify>
    <automated>node scripts/verify.js && node -e "const S=require('./sim.js');const r=S.runOrderingCheck({});console.log('checkpoint post-condition: ordering checks pass =',r.pass);process.exit(r.pass?0:1)"</automated>
  </verify>
  <done>A human has confirmed in a real browser that the victim reacts first, mind-inspector entries are not duplicated, and nothing in this codebase throws; the disposition is recorded verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future agents | `CLAUDE.md` and `PERSON-MODEL.md` are read as authoritative by every later phase's planner and executor |
| local static server → browser | `python -m http.server` on localhost, serving only this repo's static files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Repudiation | doc/code drift silently reintroduced | mitigate | Every added doc claim is grep-checkable against `sim.js` in this task's acceptance criteria |
| T-02-18 | Tampering | hand-editing generated codebase maps | mitigate | `git status --short -- .planning/codebase/` must be empty; corrections go through `/gsd:map-codebase` regeneration per the 01-03 precedent |
| T-02-19 | Information disclosure | local static server on port 8000 | accept | Serves only this repo's static prototype files on localhost, no server-side code, no secrets in the repo; the same setup Phase 1 used |
| T-02-20 | Tampering | npm/pip/cargo installs | n/a | This phase installs nothing — the project has no `package.json` and zero dependencies |
</threat_model>

<verification>
1. `node scripts/verify.js` exits 0 with `OVERALL: PASS`.
2. Every doc claim added in Task 1 is grep-verifiable against `sim.js`.
3. `git status --short -- .planning/codebase/` is empty.
4. The human checkpoint disposition is recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
- `CLAUDE.md` and `PERSON-MODEL.md` describe the engine as it now behaves, with no stale
  sentence about agent-list dispatch, in-line gossip rolls, or the absence of a verification
  command.
- A human has confirmed the reordered dispatch reads correctly in the live app.
</success_criteria>

<output>
Create `.planning/phases/02-witness-reaction-ordering/02-04-SUMMARY.md` when done. It must
record the verbatim human disposition from Task 2, the exact `witnessOrder` string the human
reported, and any code change made in response to the checkpoint.
</output>
