---
phase: 02-witness-reaction-ordering
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - sim.js
  - scripts/order-baseline.json
  - scripts/baseline.json
autonomous: true
requirements: [ORDER-01, ORDER-02]

must_haves:
  truths:
    - "When several NPCs witness the same event, the one with the highest top-candidate score is dispatched first, regardless of position in the world's agent list."
    - "An attack victim's retaliation is dispatched before any uninvolved bystander's reaction to the same event."
    - "A witness whose appraisal falls into decideAndAct's no-reaction band is dispatched last, deterministically, never ahead of a witness who will actually act."
    - "Two witnesses with equal top scores keep their agentsAt list order relative to each other, with no randomness involved."
    - "The pre-fix and post-fix dispatch orders are both committed and diffable with one command."
  artifacts:
    - path: "sim.js"
      provides: "orderWitnesses() urgency-ordering pass, wired into performAction; ORDER-01 qualitative checks in runOrderingCheck"
      contains: "function orderWitnesses"
    - path: "scripts/order-baseline.json"
      provides: "Re-blessed post-fix ordering golden master"
      contains: "witnessOrder"
  key_links:
    - from: "sim.js performAction"
      to: "sim.js orderWitnesses"
      via: "wraps computeWitnesses output before the dispatch loop"
      pattern: "orderWitnesses\\(world, event"
    - from: "sim.js orderWitnesses"
      to: "sim.js scoreCandidates"
      via: "read-only pre-pass, one call per candidate witness"
      pattern: "scoreCandidates\\("
    - from: "sim.js runOrderingCheck"
      to: "ORDER_SPEC.agentListOrder"
      via: "assertion that dispatch order diverges from agent-list order"
      pattern: "agentListOrder"
---

<objective>
Replace `performAction`'s agent-list-order witness dispatch with a score-then-dispatch pass:
score every witness's candidate reactions first, sort by urgency, then run each witness's
`perceiveEvent` cascade in that order (CONTEXT.md D-03). Then prove the change with the ORDER-01
qualitative checks and the ORDER-02 before/after order diff.

Purpose: this is the phase's actual fix. Everything before it existed to make the change
provable (Plan 02-01) and safe to compute twice (Plan 02-02).

Output: `orderWitnesses(world, event, witnessIds)` in `sim.js`, wired into `performAction`;
four new checks in `runOrderingCheck`; re-blessed `scripts/order-baseline.json`; the verbatim
pre-fix → post-fix order diff recorded as ORDER-02 evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-witness-reaction-ordering/02-CONTEXT.md
@.planning/phases/02-witness-reaction-ordering/02-PATTERNS.md
@.planning/phases/02-witness-reaction-ordering/02-01-SUMMARY.md
@.planning/phases/02-witness-reaction-ordering/02-02-SUMMARY.md

<interfaces>
Available after Plan 02-02:
- `scoreCandidates(world, witness, event, appraisal, priorRelationship)` → `{ reacts, candidates }`.
  Pure, RNG-free, idempotent. `candidates` is pre-sorted descending by `score` and always
  non-empty; when `reacts` is false it holds exactly the single `do nothing` candidate.
- `appraiseEvent(world, witness, event)` → `{ isVictim, impact, event }`. Pure.
- Both are exported on `Sim`.

Available after Plan 02-01:
- `event.witnessOrder` — a copy of the dispatched witness id array, set in `performAction`.
- `ORDER_SPEC` (module-private) with `attackerId: 'player'`, `victimId: 'garrick'`,
  `indifferentId: 'ives'`, `agentListOrder: ['mara','ives','tomas','elena','garrick']`.
- `buildOrderingScenario()` → `{ world, eventId }`; `orderingSnapshot(world, originEventId)` →
  `{ witnessOrder, reactions }`; `runOrderingCheck(opts)` → `{ pass, checks, snapshot, diffs?, prefixDiffs?, prefixLabel? }`.

Unchanged and load-bearing:
- `computeWitnesses(world, event)` (`sim.js:374-377`) still returns `agentsAt`-order ids. This
  phase does not change it — it stops *relying* on it for sequencing.
- `reactionDepth` / `MAX_REACTION_DEPTH` (`sim.js:263-264`) and the `reactedEventIds` +
  try/finally gate inside `perceiveEvent` (`sim.js:539-547`) are unchanged. `reactionDepth` is
  the one sanctioned piece of module-level mutable state in this codebase; do not add a second.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add orderWitnesses() and dispatch witnesses in urgency order</name>
  <files>sim.js</files>
  <read_first>
    - `sim.js` lines 261-291 (`reactionDepth`, `MAX_REACTION_DEPTH`, `performAction`, the `event.witnessOrder` assignment from Plan 02-01, and the `witnesses.forEach` dispatch)
    - `sim.js` lines 374-377 (`computeWitnesses`) and lines 194-196 (`agentsAt`)
    - `sim.js` lines 500-548 (`perceiveEvent` — where `appraisal` and `priorRelationship` are computed, and the reaction gate)
    - `sim.js` — `scoreCandidates` and `decideAndAct` as left by Plan 02-02
    - `sim.js` lines 91-103 (`relOf`'s lazy default creation) and lines 183-187 (`getAgent`)
    - `.planning/phases/02-witness-reaction-ordering/02-CONTEXT.md` D-02, D-03, D-04
    - `.planning/codebase/CONVENTIONS.md` (the `reactionDepth` module-state note)
  </read_first>
  <action>
    Add `function orderWitnesses(world, event, witnessIds)` immediately above `computeWitnesses`,
    returning a new array of witness ids in dispatch order. Do not mutate the input array. Then
    wire it into `performAction`: replace `const witnesses = computeWitnesses(world, event);`
    with `const witnesses = orderWitnesses(world, event, computeWitnesses(world, event));`,
    leaving the existing `event.witnessOrder = witnesses.slice();` assignment and the
    `witnesses.forEach(w => perceiveEvent(world, w, event));` dispatch line exactly as they are.
    That substitution is the entire behavioral change of this phase.

    `orderWitnesses` builds one array of `{ id, reacts, top }` by mapping over `witnessIds` in
    their incoming order, then sorts it with a two-key comparator and returns the ids.

    Per-witness scoring rules, each mirroring a real gate in `perceiveEvent` so the pre-pass
    never scores a witness who provably cannot act:
      - If `reactionDepth >= MAX_REACTION_DEPTH`, return `witnessIds.slice()` immediately
        without scoring anyone. At max depth `perceiveEvent`'s reaction gate skips every witness
        regardless of score, so scoring them is wasted work on state nobody will read.
      - `const w = getAgent(world, id);` — if `w.isPlayer` (the player's `mind` is `null`, and
        `perceiveEvent` returns before doing anything for them) or
        `w.mind.reactedEventIds.has(event.id)`, record `{ id, reacts: false, top: 0 }` without
        calling `appraiseEvent` or `scoreCandidates`.
      - Otherwise `const appraisal = appraiseEvent(world, w, event);` and
        `const { reacts, candidates } = scoreCandidates(world, w, event, appraisal, { ...relOf(w, event.actor) });`
        then record `{ id, reacts, top: candidates[0].score }`. `candidates` is pre-sorted and
        never empty, so `candidates[0]` is always the top candidate — this is D-02's definition
        of urgency, the same number `decideAndAct` would use to pick its reaction, not a
        separately invented metric.

    Comparator: `(a, b) => (a.reacts === b.reacts ? b.top - a.top : (a.reacts ? -1 : 1))`.
    Primary key `reacts` descending, secondary key `top` descending. `Array.prototype.sort` is
    stable in every engine this project targets (Node 12+, all evergreen browsers), so witnesses
    with equal keys keep their incoming `agentsAt` order — that is D-04's tiebreak, satisfied
    with no extra code and no RNG, consistent with the LOCKED D-05 RNG-scope rule.

    The scoring pass must be strictly read-only with respect to the reaction machinery: it must
    not touch `reactedEventIds`, must not increment or decrement `reactionDepth`, must not call
    `perceiveEvent` or `performAction`, and must not introduce any module-level `let`. Its only
    state is the local array inside `orderWitnesses`.

    Document these two resolutions in a comment block above `orderWitnesses`, because
    `02-CONTEXT.md` deliberately left both open and `02-PATTERNS.md` flagged them for the plan
    to decide rather than infer:

    **D-06 (planner-resolved) — witnesses that never reach candidate construction.**
    `decideAndAct` returns early when `appraisal.impact >= -0.05`, so those witnesses have no
    ranked candidate list. Resolution: `scoreCandidates` still returns their `do nothing`
    candidate (so urgency is defined and inspectable for every witness), but `reacts: false`
    puts them in a bottom bucket ahead of the score comparison, and within that bucket
    `agentsAt` order is preserved. Rejected alternative, and why: ranking them by
    `doNothingScore` directly. `doNothingScore` is clamped to `[0.05, 0.65]` and computed purely
    from personality (`boldness`, `extraversion`, `agreeableness` deviations from 0.5) with no
    event term at all, so a timid bystander who barely noticed the event could score `0.65` and
    be dispatched ahead of an actual victim whose confront score is lower — directly
    contradicting ROADMAP Phase 2 success criterion 1. Also considered and rejected: extending
    the bottom bucket to *any* witness whose winning candidate has a null `action`. Those
    witnesses' scores are event-derived and therefore genuinely comparable across witnesses,
    D-02 (locked) defines their urgency as that top score, and demoting them would change
    ordering well beyond the flagged gap and add unattributable noise to the ORDER-02 diff.

    **D-07 (planner-resolved) — recompute at dispatch, do not reuse the pre-pass values.**
    `perceiveEvent`'s signature is unchanged; it keeps computing `appraisal` and
    `priorRelationship` fresh, and `decideAndAct` keeps calling `scoreCandidates` again at real
    dispatch time. The pre-pass results are used for ordering and nothing else. Rationale: an
    earlier witness's reaction cascade can change a later witness's relationship and emotion
    state between the two passes. Reusing pre-pass values would push that staleness into the
    *decision itself* — a witness would act on a world that no longer exists. Recomputing
    confines the staleness to *ordering*, which is a priority heuristic, and leaves every
    witness reacting to the world as it actually is when their turn comes, which is what a
    "beliefs, not scripts" engine should do. This costs one extra `scoreCandidates` call per
    reacting witness, which is free now that the function is pure and RNG-free (Plan 02-02).
    Record the one visible consequence so it is not later filed as a bug: a witness can be
    sorted first and then take no action, because their recomputed `impact` crossed `-0.05`
    after an earlier witness's cascade changed their affection toward the victim. That is
    accepted and inherent to D-03's two-pass shape.

    Also note in the comment that `relOf` inside the pre-pass may lazily create a default
    relationship entry slightly earlier than before; the created values are a deterministic
    function of the witness's `DangerousWorld` weight, so nothing observable changes.
  </action>
  <acceptance_criteria>
    - `grep -c 'function orderWitnesses' sim.js` outputs `1`.
    - `grep -c 'orderWitnesses(world, event, computeWitnesses(world, event))' sim.js` outputs `1`.
    - `grep -c 'witnesses.forEach' sim.js` outputs `1` and `grep -c 'event.witnessOrder = witnesses.slice();' sim.js` outputs `1` — the dispatch line and the provenance line are untouched.
    - `grep -cE '^(let|var) ' sim.js` outputs `1`.
    - The pre-pass touches none of the reaction machinery: `node -e "const fs=require('fs');const src=fs.readFileSync('sim.js','utf8');const s=src.indexOf('function orderWitnesses');const e=src.indexOf('function computeWitnesses');const body=src.slice(s,e);const bad=['reactedEventIds','reactionDepth','perceiveEvent(','performAction('].filter(t=>body.includes(t));console.log('forbidden tokens inside orderWitnesses:',JSON.stringify(bad));process.exit(bad.length===0?0:1)"` exits 0.
    - Victim first, indifferent witness last: `node -e "const S=require('./sim.js');const s=S.buildOrderingScenario();const ev=s.world.events.find(e=>e.id===s.eventId);const o=ev.witnessOrder;console.log('dispatch order:',o.join(','));process.exit(o[0]==='garrick'&&o[o.length-1]==='ives'&&o.length===5?0:1)"` exits 0.
    - Order genuinely diverges from agent-list order: the same harness confirms `o.join(',') !== 'mara,ives,tomas,elena,garrick'` while `o.slice().sort().join(',')` equals `'elena,garrick,ives,mara,tomas'` (same set, different sequence).
    - Victim retaliates first: `node -e "const S=require('./sim.js');const s=S.buildOrderingScenario();const snap=S.orderingSnapshot(s.world,s.eventId);const f=snap.reactions[0];console.log('first reaction:',JSON.stringify(f));process.exit(f&&f.causedBy===s.eventId&&f.actor==='garrick'&&f.verb==='Attack'&&f.target==='player'?0:1)"` exits 0. If this fails, **stop and report — do not tune anything.** Specifically: do not change `ORDER_SPEC`, do not raise `garrick`'s boldness, do not weaken the assertion to accept a non-`Attack` reaction, and do not run `--capture-prefix-order`. This plan runs post-fix dispatch, so any prefix captured here would record post-fix ordering and destroy the ORDER-02 "before" side rather than restore it; and changing `ORDER_SPEC` now would leave `scripts/order-prefix.json` describing a scenario that no longer exists, conflating scenario retuning with the ordering fix. The confront-vs-gossip margin was pinned in Plan 02-01 Task 2; `garrick`'s `considered` array may have legitimately drifted since then per 02-02's own re-bless allowlist (RNG-deferral-driven damage/log changes are expected there), so `02-02-SUMMARY.md`'s recorded value — not 02-01's — is the correct comparison basis if present, falling back to `02-01-SUMMARY.md` only if 02-02 never recorded one. A failure here therefore means one of exactly two things, both reportable: (a) `orderWitnesses` is scoring or sorting wrong — the likely case, and a real bug in this task's own code; or (b) the margin genuinely moved because `garrick` now reacts *first*, against an un-cascaded world, rather than last after four cascades. Diagnose by printing the full five-witness pre-pass table (`{id, reacts, top score}` for every witness in `snap`, not just `garrick`'s entry) alongside `garrick`'s `mind.log` entry for the origin event, comparing its `considered` array term by term against the most recent recorded numbers; report both the table and the comparison, state which of (a) or (b) it indicates, and halt. Resolving (b) requires re-pinning the margin in 02-01 and re-freezing the prefix from pre-fix code, which is a replanning decision, not something this task may do inline.
    - Determinism across repeat builds: `node -e "const S=require('./sim.js');const a=S.buildOrderingScenario(),b=S.buildOrderingScenario();const x=JSON.stringify(S.orderingSnapshot(a.world,a.eventId)),y=JSON.stringify(S.orderingSnapshot(b.world,b.eventId));process.exit(x===y?0:1)"` exits 0.
    - Depth guard: `node -e "const S=require('./sim.js');const s=S.buildOrderingScenario();const depths=s.world.events.map(e=>e.witnessOrder.length);console.log('witnessOrder lengths:',depths.join(','));process.exit(depths.every(n=>typeof n==='number')?0:1)"` exits 0 and the SUMMARY records that no run threw a `TypeError` from a player witness lacking `mind`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const S=require('./sim.js');const s=S.buildOrderingScenario();const ev=s.world.events.find(e=>e.id===s.eventId);const o=ev.witnessOrder;const snap=S.orderingSnapshot(s.world,s.eventId);const f=snap.reactions[0];const ok=o[0]==='garrick'&&o[o.length-1]==='ives'&&o.join(',')!=='mara,ives,tomas,elena,garrick'&&f&&f.actor==='garrick'&&f.verb==='Attack'&&f.target==='player';console.log('order:',o.join(','),'| first reaction:',f&&f.actor+':'+f.verb+'->'+f.target);process.exit(ok?0:1)"</automated>
  </verify>
  <done>Witnesses are dispatched highest-urgency-first with a stable `agentsAt` tiebreak and a deterministic bottom bucket for non-reacting witnesses; the victim retaliates before any bystander reacts; both open decisions are resolved in an in-code comment.</done>
</task>

<task type="auto">
  <name>Task 2: Add the ORDER-01 qualitative checks, re-bless both baselines, and capture the ORDER-02 diff</name>
  <files>sim.js, scripts/order-baseline.json, scripts/baseline.json</files>
  <read_first>
    - `sim.js` — `ORDER_SPEC`, `buildOrderingScenario`, `orderingSnapshot`, `runOrderingCheck` as left by Plan 02-01
    - `sim.js` lines 1289-1397 (`runRegressionCheck` — the check-object shape `{ name, pass, detail }` and its "never escalatable vs. behavioral claim" comment convention)
    - `scripts/verify.js` in full — how ordering checks are printed, how they feed `plainRunPasses`, and the `--update-baseline` refusal gate
    - `.planning/phases/01-verification-infrastructure/01-02-SUMMARY.md` §"Decisions Made" (why checks are scoped by `causedBy` rather than "any event by the agent")
  </read_first>
  <action>
    Add four qualitative checks to `runOrderingCheck`, each a `{ name, pass, detail }` object
    pushed before the existing `order-matches-baseline` check, each with a `detail` that carries
    the observed values verbatim so a failure is diagnosable from the printed line alone:

      1. `dispatch-order-differs-from-agent-list` — `snapshot.witnessOrder` contains exactly the
         same ids as `ORDER_SPEC.agentListOrder` but in a different sequence. `detail` prints
         both sequences. This is the check that makes "the fix actually did something" explicit
         rather than inferred from a diff.
      2. `victim-dispatched-first` — `snapshot.witnessOrder[0] === ORDER_SPEC.victimId`. `detail`
         names the observed first witness and states that agent-list order would have put
         `ORDER_SPEC.agentListOrder[0]` there instead.
      3. `victim-retaliates-first` — the first entry of `snapshot.reactions` has
         `causedBy === originEventId`, `actor === ORDER_SPEC.victimId`, `verb === 'Attack'`, and
         `target === ORDER_SPEC.attackerId`. Scope it by `causedBy` rather than "the first Attack
         by the victim anywhere", matching the pattern 01-02 established. This is ROADMAP Phase 2
         success criterion 1, asserted directly.
      4. `indifferent-witness-dispatched-last` — the last entry of `snapshot.witnessOrder` is
         `ORDER_SPEC.indifferentId`. `detail` states this witness's appraised impact falls in
         `decideAndAct`'s no-reaction band and records its position in `agentListOrder` (index 1
         of 5) so the demotion is visible as a real reordering. This is the observable proof of
         D-06.

    Keep `order-matches-baseline` last and keep it never-acknowledgeable, exactly as
    `scripts/verify.js` already enforces. Do not add ORDER-01 assertions to
    `runRegressionCheck` — the two-clone scenario is a different fixture with a different job.

    Then re-bless the golden masters. Run `node scripts/verify.js` first: it will print
    `FAIL order-matches-baseline` (expected — the ordering changed on purpose), the rendered
    `Ordering baseline diff:`, and the rendered
    `Ordering fix effect (<prefix label> -> current):` section. Before re-blessing, confirm all
    four new ordering checks and all five Phase 1 qualitative checks print `PASS`. That is the
    gate: `verify.js`'s `REFUSED` path already refuses to write while any non-baseline check is
    unacknowledged-failing, so do not bypass it, and do not create a
    `scripts/known-mismatch.json` to get past a failure — a failing qualitative check here means
    the fix is wrong, not that the baseline is stale.

    Capture the full `Ordering fix effect` section verbatim into the SUMMARY *before* running
    `--update-baseline`. That block is ORDER-02's deliverable: the concrete, field-by-field
    demonstration that the fix changed dispatch order and reaction sequence, sourced from a
    fixture captured on pre-fix code rather than assumed.

    Then run `node scripts/verify.js --update-baseline`, which rewrites both
    `scripts/order-baseline.json` and `scripts/baseline.json`. `scripts/order-prefix.json` must
    remain untouched — it is the frozen "before" side and `--update-baseline` never writes it.
    Confirm with `git status --short` that exactly those two JSON files plus `sim.js` changed.

    For `scripts/baseline.json`: the two-clone scenario's witness set is small (`mara`, `ives`,
    with everyone else relocated to `'away'`), so its ordering may or may not change. Whatever
    the diff is, the gate is the same and is already enforced: all five qualitative checks must
    print `PASS`, meaning the `CompetitiveJungle` divergence — the claim that fixture exists to
    protect — survived the ordering change. Record the verbatim diff in the SUMMARY.

    Finally re-run `node scripts/verify.js` and confirm it exits 0 with `OVERALL: PASS`, with
    the `Ordering fix effect` section now printing the real before/after diff on every future
    run.
  </action>
  <acceptance_criteria>
    - `node scripts/verify.js` exits 0 and its output contains all of: `PASS dispatch-order-differs-from-agent-list`, `PASS victim-dispatched-first`, `PASS victim-retaliates-first`, `PASS indifferent-witness-dispatched-last`, `PASS order-matches-baseline`, and `OVERALL: PASS`.
    - The same output still contains all five Phase 1 checks as `PASS`: `clone-specs-differ-in-exactly-one-field`, `drift-disabled`, `positive-clone-attacks-player`, `negative-clone-takes-no-action`, `reactions-diverge`.
    - The `Ordering fix effect (... -> current):` section is present and is **not** `no differences`; its verbatim content appears in the SUMMARY, and it includes at least one `order.witnessOrder.N: <id> -> <id>` line.
    - `node -e "const S=require('./sim.js');const r=S.runOrderingCheck({});const names=r.checks.map(c=>c.name);console.log(names.join(','));process.exit(names.length===4&&r.pass?0:1)"` exits 0 — without a baseline option, exactly the four qualitative checks run and all pass.
    - `node -e "const p=require('./scripts/order-prefix.json');const b=require('./scripts/order-baseline.json');console.log('prefix:',p.snapshot.witnessOrder.join(','));console.log('current:',b.witnessOrder.join(','));process.exit(JSON.stringify(p.snapshot.witnessOrder)!==JSON.stringify(b.witnessOrder)?0:1)"` exits 0 — the frozen before and the re-blessed after genuinely differ.
    - `git status --short` after the re-bless is clean (everything committed), and `git show --stat HEAD` lists exactly `sim.js`, `scripts/order-baseline.json`, and `scripts/baseline.json`.
    - Acknowledgement rejection still holds: a temporary `scripts/known-mismatch.json` containing `{"approved":true,"acknowledged":["order-matches-baseline"]}` makes `node scripts/verify.js` exit 1 at load time. Delete it afterwards and confirm `git status --short` is clean.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify.js && node -e "const p=require('./scripts/order-prefix.json');const b=require('./scripts/order-baseline.json');process.exit(JSON.stringify(p.snapshot.witnessOrder)!==JSON.stringify(b.witnessOrder)?0:1)"</automated>
  </verify>
  <done>Four ORDER-01 qualitative checks pass, both golden masters are re-blessed with all qualitative checks green, and the verbatim pre-fix → post-fix ordering diff is captured as ORDER-02 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-pass scoring → world state | A read-only pass over every witness that runs before any mutation; a hidden write here corrupts every later witness's inputs |
| reordered dispatch → recursion gate | `reactionDepth` / `reactedEventIds` must keep bounding cascades under a new dispatch sequence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of service | unbounded reaction cascade under reordered dispatch | mitigate | `orderWitnesses` returns unsorted immediately at `reactionDepth >= MAX_REACTION_DEPTH`; the `reactedEventIds` + try/finally gate in `perceiveEvent` is untouched; source-slice check forbids `reactionDepth`/`reactedEventIds` inside `orderWitnesses` |
| T-02-12 | Tampering | pre-pass mutating state the real dispatch then reads | mitigate | `scoreCandidates` purity is asserted by Plan 02-02's double-call harness; `orderWitnesses` calls only `getAgent`, `appraiseEvent`, `relOf`, and `scoreCandidates` |
| T-02-13 | Elevation of privilege | randomness deciding dispatch order | mitigate | Ordering reads only RNG-free scores; ties fall back to stable-sort `agentsAt` order (D-04), honoring the LOCKED D-05 RNG-scope rule |
| T-02-14 | Repudiation | ordering change blessed without evidence | mitigate | `scripts/order-prefix.json` is frozen pre-fix and never rewritten by `--update-baseline`; the fix-effect diff prints on every run and is recorded verbatim in the SUMMARY |
| T-02-15 | Spoofing | a player agent scored as an NPC | mitigate | `orderWitnesses` checks `w.isPlayer` before touching `w.mind`, mirroring `perceiveEvent`'s own guard; the player has `mind: null` |
| T-02-16 | Tampering | npm/pip/cargo installs | n/a | This phase installs nothing — the project has no `package.json` and zero dependencies |
</threat_model>

<verification>
1. `node scripts/verify.js` exits 0 with `OVERALL: PASS`, all five Phase 1 checks and all four
   new ordering checks passing.
2. The scripted scenario's dispatch order is `garrick` first and `ives` last, differing from
   `mara,ives,tomas,elena,garrick`.
3. The first event caused by the origin is `garrick` attacking `player`.
4. `scripts/order-prefix.json` and `scripts/order-baseline.json` differ, and the difference
   prints as a rendered diff on every verify run.
5. `grep -cE '^(let|var) ' sim.js` outputs `1`; `computeWitnesses` and `agentsAt` are unmodified.
</verification>

<success_criteria>
- ORDER-01: witnesses react in computed-urgency order, victim before bystanders, with a
  deterministic bottom bucket and a no-RNG stable tiebreak.
- ORDER-02: pre-fix and post-fix orders are both committed and diffed, with the diff recorded
  verbatim.
- Both open decisions (D-06, D-07) are resolved in an in-code comment with their rejected
  alternatives and the reasoning, not silently picked.
</success_criteria>

<output>
Create `.planning/phases/02-witness-reaction-ordering/02-03-SUMMARY.md` when done. It must
record verbatim: the full `Ordering fix effect` diff section, the pre-fix and post-fix
`witnessOrder` arrays side by side, the `scripts/baseline.json` re-bless diff, and
`garrick`'s `considered` array for the origin event compared term by term against the
most recently recorded numbers (`02-02-SUMMARY.md` if it recorded them, else
`02-01-SUMMARY.md`) — the pinned margin this plan re-verifies, forbidden from re-tuning
`ORDER_SPEC` or re-capturing `scripts/order-prefix.json`. If Task 1's stop-and-report branch
fires, also record the full five-witness pre-pass table it prints.
</output>
