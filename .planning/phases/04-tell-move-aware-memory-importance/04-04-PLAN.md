---
phase: 04-tell-move-aware-memory-importance
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - scripts/baseline.json
  - scripts/order-baseline.json
  - PERSON-MODEL.md
autonomous: false
requirements: [MEMORY-01, MEMORY-02]

must_haves:
  truths:
    - "`node scripts/verify.js` exits 0 with OVERALL: PASS and every check green"
    - "The re-blessed baselines were reviewed against an explained diff, not blindly captured"
    - "PERSON-MODEL.md no longer claims appraiseEvent has no branch for Tell or Move, or that every conversation-memory forms at the 0.1 floor regardless of content"
    - "A human confirms the mind inspector renders memories (including above-1.0 importance values) in the browser without errors, and that being told severe news is visibly more memorable than being told something mundane"
    - "D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08: doc sync reflects the actual shipped mechanisms, including the boost (not damping) form of claim.subject scaling and the elena-scoped Move check"
  artifacts:
    - path: "scripts/baseline.json"
      provides: "Re-blessed golden master reflecting TELL_SEVERITY-scaled Tell memories, causally-detected Move flight memories, and the raised maxMemoryImportance ceiling"
      contains: "\"importance\""
    - path: "PERSON-MODEL.md"
      provides: "Updated Memories section and Gaps section reflecting the real Tell/Move appraisal behavior"
      contains: "TELL_SEVERITY"
  key_links:
    - from: "PERSON-MODEL.md Memories section"
      to: "sim.js appraiseEvent Tell/Move branches"
      via: "documents TELL_SEVERITY, the boost-form claim.subject scaling, the causedBy flight trace, and TUNING.maxMemoryImportance"
      pattern: "TELL_SEVERITY"
---

<objective>
Close the phase: confirm every check is green under the final Tell/Move appraisal logic,
perform the single human-reviewed golden-master re-bless, bring `PERSON-MODEL.md` back in
sync with the code, and have a human confirm the browser UI actually works with the new,
sometimes-above-1.0 memory importance values.

Purpose: Plans 04-01 through 04-03 were each forbidden from running `--update-baseline`
because the Tell/Move impact changes and the raised ceiling touch essentially every
Attack-victim and conversation memory in both golden-master fixtures — that blast radius
was predicted by `04-PATTERNS.md`'s "Baseline blast radius" section and can only be judged
once all three prior plans are live together (this plan is the one sanctioned re-bless,
following the exact staged pattern `03-05-PLAN.md` used for Phase 3). `CLAUDE.md` also
treats drift between `PERSON-MODEL.md` and `sim.js` as a bug in one or the other, and this
phase falsifies two specific documented claims — fixing them is a phase deliverable, not
housekeeping.

Output: re-blessed `scripts/baseline.json` and `scripts/order-baseline.json`, an updated
`PERSON-MODEL.md`, and a human browser verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@PERSON-MODEL.md
@.planning/phases/04-tell-move-aware-memory-importance/04-CONTEXT.md
@.planning/phases/04-tell-move-aware-memory-importance/04-PATTERNS.md
@.planning/phases/04-tell-move-aware-memory-importance/04-01-PLAN.md
@.planning/phases/04-tell-move-aware-memory-importance/04-02-PLAN.md
@.planning/phases/04-tell-move-aware-memory-importance/04-03-PLAN.md
@.planning/phases/04-tell-move-aware-memory-importance/04-01-SUMMARY.md
@.planning/phases/04-tell-move-aware-memory-importance/04-02-SUMMARY.md
@.planning/phases/04-tell-move-aware-memory-importance/04-03-SUMMARY.md

**Re-bless mechanics (from `scripts/verify.js`, same gate Phase 3's `03-05-PLAN.md` used):**

- `node scripts/verify.js --update-baseline` REFUSES (exit 1) while any *qualitative* check
  is failing unacknowledged. The two golden-master checks (`snapshot-matches-baseline`,
  `order-matches-baseline`) are exempt from that refusal gate precisely so a legitimate
  structural change like this phase's can be blessed at all — and neither can ever be
  silenced via `known-mismatch.json`.
- The flag writes BOTH `scripts/baseline.json` and `scripts/order-baseline.json`. It must
  NOT write `scripts/order-prefix.json`, which is Phase 2's frozen pre-fix "before" side and
  is only ever produced by `--capture-prefix-order`.
- `scripts/known-mismatch.json` does not exist and must not be created by this plan.

**Documentation drift this phase created — exact anchors:**

- `PERSON-MODEL.md:231-237` — "`importance` is fixed at formation from `appraisal.impact`,
  which is only ever nonzero for Take/Attack/Give. **`appraiseEvent` has no branch for Tell
  or Move**, so every conversation-memory forms at the floor value (`0.1`) no matter how
  explosive the content was. Hearing "X is dead" decays exactly as fast as hearing about the
  weather. Real gap, not a design choice..."
- `PERSON-MODEL.md:509-511` — the "Pre-existing stubs" list entry: "**Tell/Move-aware memory
  importance.** Conversation memories should presumably inherit some signal from the claim's
  content, not always form at the floor."
- `CLAUDE.md` was checked and contains no false claim about Tell/Move memory importance — its
  mind-box table's `memories` row already says only "computed live (`memoryStrength`)" without
  naming the flat-floor gap, and its "Known gaps" paragraph never listed this item as a
  headline gap. **`CLAUDE.md` needs no edit in this plan** — do not touch it; `files_modified`
  intentionally omits it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite confirmation and the single human-reviewed golden-master re-bless</name>

  <read_first>
    - `scripts/verify.js:106-160` — `main()`'s check batches, both diff prints, and the section headers
    - `scripts/verify.js:220-255` — the refusal gate, `NON_ACKNOWLEDGEABLE_CHECK_NAMES`, and both baseline writes
    - `.planning/phases/04-tell-move-aware-memory-importance/04-03-SUMMARY.md` — the final Baseline diff prediction from Task 3's live regression check
    - `.planning/phases/03-belief-decay-needs-regeneration/03-05-SUMMARY.md` — the previous re-bless diff and classification, for comparison
    - `.planning/phases/04-tell-move-aware-memory-importance/04-PATTERNS.md` §"Baseline blast radius" — the exact mechanisms predicted for this phase's diff
  </read_first>

  <files>scripts/baseline.json, scripts/order-baseline.json</files>

  <action>
Run `node scripts/verify.js` and capture the complete output verbatim.

Before blessing anything, confirm and record that every qualitative check is green: the five
Phase 1 checks, the four ORDER-01 checks, the fourteen DECAY checks, and the five
MEMORY-01/MEMORY-02 checks (`tell-severity-scales-above-floor`,
`tell-severity-orders-by-predicate`, `move-flight-scales-above-floor`,
`ordinary-move-stays-at-floor`, `high-importance-memory-outlives-old-ceiling`). The only
permitted red lines are `snapshot-matches-baseline` and `order-matches-baseline`.

Review the printed `Baseline diff:` block against the mechanisms `04-PATTERNS.md`'s "Baseline
blast radius" section predicted, and classify every diff entry as expected-and-explained
before blessing:
  1. Any direct Attack VICTIM's stored `mind.memories.<index>.importance` moving from the old
     clamped `1` up to `1.2` or higher — the `isVictim` block-skip mechanism `04-PATTERNS.md`
     identified, not a "high-Safety-witness" case.
  2. Any `mind.memories.<index>.importance` entry newly present or changed on a Tell-forming
     memory — D-01/D-02/D-06/D-07's severity-scaled Tell impact.
  3. Any `mind.memories.<index>.importance` entry on a Move-forming memory — D-04/D-08's
     causal flight detection (expect this only for witnesses whose `generalCareOf` clears the
     floor at the `-0.5` base; an ordinary or low-`generalCareOf`-witnessed Move should show NO
     diff, still forming at `0.1` exactly as before).
  4. Any `witnessOrder` / reaction change in the ordering fixture — must be explained against
     Plan 04-03 Task 3's own live-regression confirmation that ORDER-01 held.
If any diff entry fits none of those four categories, STOP and report it rather than
blessing: an unexplained entry is exactly what the golden master exists to catch.

Then run `node scripts/verify.js --update-baseline` once, and re-run plain
`node scripts/verify.js` to confirm it now exits 0 with `OVERALL: PASS`.

Confirm `git status --short` shows `scripts/baseline.json` and `scripts/order-baseline.json`
modified and `scripts/order-prefix.json` UNCHANGED — Phase 2's frozen pre-fix comparison must
survive this phase intact.

Do not create `scripts/known-mismatch.json`. Do not modify `sim.js` in this plan at all — the
severity table, flight-impact magnitude, and importance ceiling are all locked constants
(D-01/D-06/D-07/D-08/D-05) that Plans 04-01/04-02/04-03 already verified against `ORDER_SPEC`
and the fixture agents. If a value looks wrong here, that is a stop-and-report condition, not
an edit.
  </action>

  <verify>
    <automated>node scripts/verify.js > /dev/null 2>&1; echo "exit=$?"; node scripts/verify.js | tail -1</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/verify.js` exits 0 and its last line is exactly `OVERALL: PASS`
    - The full pre-bless output, including the complete `Baseline diff:` block, is recorded verbatim in the summary with every entry classified into one of the four predicted mechanisms
    - `node -e "const S=require('./sim.js');const a=[].concat(S.runRegressionCheck({}).checks,S.runOrderingCheck({}).checks,S.runDecayCheck().checks,S.runMemoryImportanceCheck().checks);const bad=a.filter(c=>!c.pass);console.log(bad.length===0?'ALL GREEN '+a.length:'RED: '+bad.map(c=>c.name).join(','));"` prints `ALL GREEN` with a count of at least 27
    - `git status --short scripts/` shows `scripts/baseline.json` and `scripts/order-baseline.json` modified; `git diff --stat scripts/order-prefix.json` is empty
    - `ls scripts/known-mismatch.json` fails (the file was never created)
    - `node -e "const fs=require('fs');const b=JSON.parse(fs.readFileSync('scripts/baseline.json','utf8'));const s=JSON.stringify(b);const above=/\"importance\":\s*1\.[1-9]/.test(s);if(!above)throw new Error('no above-1.0 importance value found in re-blessed baseline');console.log('OK above-1.0 importance present')"` prints `OK` (confirms D-05's ceiling actually shows up in the blessed fixture, not just in an isolated unit check)
  </acceptance_criteria>

  <done>Every check is green, the two baselines are re-blessed after a diff review that classified every entry, `order-prefix.json` is untouched, and no check was acknowledged away.</done>
</task>

<task type="auto">
  <name>Task 2: Doc sync — PERSON-MODEL.md brought back in step with sim.js</name>

  <read_first>
    - `PERSON-MODEL.md:220-247` — the Memories section (the stale claim at 231-237)
    - `PERSON-MODEL.md:495-518` — the "Pre-existing stubs" gaps list, including the stale entry at 509-511
    - `sim.js` — the final `TELL_SEVERITY` table, `appraiseEvent`'s `Tell` and `Move` arms, and the final `TUNING.maxMemoryImportance` value, so the doc describes what was actually built rather than what CONTEXT.md proposed
    - All four summaries in this phase (04-01 through 04-03), for the exact final constants and any recorded deviations
  </read_first>

  <files>PERSON-MODEL.md</files>

  <action>
Update `PERSON-MODEL.md` so no claim contradicts `sim.js`. Required edits:

  - **Memories section (~lines 231-237):** replace "`importance` is fixed at formation from
    `appraisal.impact`, which is only ever nonzero for Take/Attack/Give. **`appraiseEvent` has
    no branch for Tell or Move**, so every conversation-memory forms at the floor value (`0.1`)
    no matter how explosive the content was..." with the real behavior:
    - `appraiseEvent` now has a `Tell` branch: impact is looked up from `TELL_SEVERITY` (a
      per-predicate base magnitude table beside `PREDICATE_LABELS`), signed (`is_trustworthy`
      positive, the other five negative), multiplied by the same direct/overheard
      confidence-in-the-claim formula `perceiveEvent` already computes (duplicated inline for
      purity — `appraiseEvent` must compute identically from both the `orderWitnesses` pre-pass
      and `perceiveEvent`'s real dispatch), and further scaled UP (boost form, not damping) by
      `relOf(witness, claim.subject).affection` when the witness cares about who the claim is
      about.
    - `appraiseEvent` now has a `Move` branch: impact stays `0` (the same flat floor as before)
      unless the Move is causally a flight from violence — `event.causedBy` traces to an event
      where the mover was the target of an `Attack` or a non-consented `Take` — in which case a
      moderate negative base impact applies, further scaled by the witness's `generalCareOf`
      through the same generic block every verb passes through (state plainly that this means
      only witnesses with above-fixture-median empathy reliably clear the `0.1` floor for an
      ordinary Move — an apathetic bystander not forming a vivid memory of a stranger fleeing is
      accepted as correct, not a residual gap).
    - The importance ceiling in `perceiveEvent`'s `addMemory` call was raised from the literal
      `1` to `TUNING.maxMemoryImportance`, applied uniformly to every event type — note that a
      direct Attack VICTIM's un-damped impact already exceeded the old ceiling before this
      phase (the `isVictim` block-skip), so some previously-invisible severity is now visible
      too, not just Tell/Move's new signal.
  - **Gaps section (~lines 509-511):** remove the "**Tell/Move-aware memory importance.**
    Conversation memories should presumably inherit some signal from the claim's content, not
    always form at the floor." entry from the "Pre-existing stubs" list — it is now shipped.
  - Name the final `TUNING.maxMemoryImportance` value and the six `TELL_SEVERITY` values in
    the updated section (read them from `sim.js`, do not guess or copy CONTEXT.md's proposed
    numbers if the shipped values differ).

Keep the document in its existing voice and structure. Do not restructure sections, do not add
a changelog, and do not describe anything that was not actually built — read the three prior
summaries for what shipped, including any recorded deviation.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const pm=fs.readFileSync('PERSON-MODEL.md','utf8');const stale=[['appraiseEvent has no branch',/appraiseEvent.{0,20}has no branch/i],['forms at the floor no matter',/no matter how explosive/i],['Tell/Move stub gap entry',/Conversation memories should presumably inherit/]];const found=stale.filter(([l,re])=>re.test(pm));if(found.length)throw new Error('stale claims remain: '+found.map(x=>x[0]).join(' | '));for(const t of ['TELL_SEVERITY','maxMemoryImportance','causedBy'])if(!pm.includes(t))throw new Error('PERSON-MODEL.md missing '+t);console.log('OK');"</automated>
  </verify>

  <acceptance_criteria>
    - The automated command prints `OK` and exits 0 (all three stale claims gone, new mechanisms named)
    - `grep -n 'TELL_SEVERITY\|maxMemoryImportance\|causedBy' PERSON-MODEL.md` returns at least one hit each, documenting the actual shipped mechanism
    - `PERSON-MODEL.md` states plainly that the boost form (not damping) is used for `claim.subject` affection scaling, and that ordinary/low-empathy-witnessed Moves still floor-collapse to `0.1` as accepted behavior
    - `node scripts/verify.js` still exits 0 with `OVERALL: PASS` (docs-only change, no behavior touched)
    - `git status --short` shows only `PERSON-MODEL.md` modified by this task
  </acceptance_criteria>

  <done>PERSON-MODEL.md makes no claim `sim.js` contradicts, the new mechanisms and constants are documented with their real shipped values, and the Move floor-collapse behavior is recorded as an accepted, explained scope boundary rather than left as apparent drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human browser verification of memory rendering and felt severity</name>

  <read_first>
    - `presentation.js:90-94` — the Memories list render (plain-text `.toFixed(2)` strength display, no bar-fill percentage assuming a 0-1 cap)
    - `presentation.js:179-181` — the debug report's memories block
    - `index.html` — the mind-panel and debug-report controls the human will click
    - `.planning/phases/03-belief-decay-needs-regeneration/03-05-HUMAN-UAT.md` — the persisted-checkpoint precedent to follow if the human is unavailable this session
  </read_first>

  <what-built>
    `Tell` and `Move` events now produce real, severity-scaled `appraiseEvent` impact instead
    of the flat `0.1` floor, and the memory-importance ceiling was raised from `1` to
    `TUNING.maxMemoryImportance` so severe memories (including some Attack-victim memories
    that already silently exceeded the old ceiling) can now store and display importance
    values above `1.0`.

    Everything automatable has been verified: 27+ checks green via `node scripts/verify.js`,
    both baselines re-blessed after a reviewed diff, and `PERSON-MODEL.md` re-synced. What no
    automated check in this repo can cover is the actual DOM render — there is no headless
    browser here, and `presentation.js`'s memory-display path has never rendered a value above
    `1.00` before this phase.
  </what-built>

  <how-to-verify>
    1. Start the static server from the repo root: `python -m http.server 8000` (NOT
       `python3` — that's a broken Store-alias stub on this machine).
    2. Open `http://localhost:8000/index.html` and open the browser devtools Console.
    3. Type `attack garrick` (or any NPC) as the player. Open Garrick's mind inspector and
       confirm the **Memories** section shows a "still felt at" value that can read above
       `1.00` for this fresh, maximally-strong memory — confirm no `NaN`, no blank list, no
       Console error.
    4. Have one NPC witness tell another NPC something severe — e.g. type
       `tell mara that garrick is dead` (or the closest equivalent command the parser accepts
       for an `is_dead` claim) and confirm nothing throws in the Console.
    5. Open the told NPC's mind inspector and confirm a new Memories entry appears for the
       Tell event with a noticeably higher "still felt at" value than an ordinary, low-stakes
       action produces (compare against an NPC's memory of an unrelated `give`/`move`).
    6. Perform several more unrelated actions to advance ticks, then reopen the same NPC's
       inspector and confirm the severe-Tell memory is STILL present and still has meaningful
       strength, while a mundane memory from around the same tick has visibly faded further or
       dropped off — this is MEMORY-01/MEMORY-02 observed by eye.
    7. Click the debug-report button and confirm it builds without error and its `memories:`
       lines show `importance` values, including any above `1.00`, formatted normally (no
       `NaN`, no truncation).

    Report anything unexpected verbatim, including the full Console error text if any appears.
  </how-to-verify>

  <resume-signal>Type "approved" if steps 3-7 all behave as described, or describe exactly what differed (with verbatim Console output for any error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/verify.js --update-baseline` → `scripts/*.json` | The one point in the phase where the regression gate's reference state is rewritten. |
| `sim.js` engine state → `presentation.js` DOM render | Verified by a human in this plan because no headless browser exists in the repo, and this is the first phase where a memory's displayed strength can exceed `1.00`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | Baseline re-blessed while hiding an unexplained regression | mitigate | Task 1 requires every diff entry be classified into one of four predicted mechanisms before blessing, and stop-and-report for anything unclassifiable; `verify.js`'s own refusal gate independently blocks blessing while any qualitative check is red. |
| T-04-18 | Repudiation | `scripts/order-prefix.json` overwritten, destroying Phase 2's frozen ORDER-02 "before" evidence | mitigate | Acceptance criterion asserts `git diff --stat scripts/order-prefix.json` is empty; `--update-baseline` does not write that path. |
| T-04-19 | Information Disclosure | Debug report pasted publicly | accept | Contains only simulated NPC state from a local prototype; no user data, credentials, or PII exist in this project. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero-dependency static site, no `package.json`, no install task in this phase. |
</threat_model>

<verification>
- `node scripts/verify.js` exits 0, last line `OVERALL: PASS`, 27+ checks green
- `scripts/order-prefix.json` unchanged; `scripts/known-mismatch.json` absent
- No stale documentation claim survives in `PERSON-MODEL.md` (automated string check)
- Human confirms browser render of above-1.0 memory importance, severe-vs-mundane Tell memory contrast, and debug report correctness
</verification>

<success_criteria>
- MEMORY-01 and MEMORY-02 both verified green in one run under the final Tell/Move logic
- The single golden-master re-bless happened after a classified diff review
- `PERSON-MODEL.md` matches `sim.js`, with the Move floor-collapse behavior documented as an accepted, explained choice
- A human has confirmed the UI works with above-1.0 memory values, closing the gap no check in this repo can cover
</success_criteria>

<output>
Create `.planning/phases/04-tell-move-aware-memory-importance/04-04-SUMMARY.md` when done.
Record verbatim: the pre-bless `node scripts/verify.js` output with the full classified
`Baseline diff:` block, the post-bless output showing `OVERALL: PASS`, the final `TUNING`
and `TELL_SEVERITY` values, and the human's verbatim checkpoint response. If the checkpoint
is not reached in this session, persist it as `04-04-HUMAN-UAT.md` following Phase 3's
`03-05-HUMAN-UAT.md` precedent.
</output>