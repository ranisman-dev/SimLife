---
phase: 01-verification-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sim.js
  - presentation.js
autonomous: true
requirements: [VERIF-02, VERIF-03]

must_haves:
  truths:
    - "Two worlds seeded with the same seed produce the same sequence of rng() values"
    - "Two worlds seeded with different seeds produce different sequences"
    - "Running the identical scripted action sequence twice under the same seed produces byte-identical event data"
    - "No bare Math.random() call remains anywhere in sim.js"
    - "A live browser session records the seed it was started with, so that session is replayable"
    - "world.driftEnabled reads as enabled when unset, and only an explicit false disables it"
  artifacts:
    - path: "sim.js"
      provides: "mulberry32 PRNG, DEFAULT_SEED, seedRng(world, seed), rngOf(world), isDriftEnabled(world), TUNING constants block"
      contains: "function seedRng"
    - path: "presentation.js"
      provides: "Seeds the RNG stream at both world-creation sites so live play is reproducible from world.seed"
      contains: "Sim.seedRng(world"
  key_links:
    - from: "sim.js applyEffects (Attack damage)"
      to: "world.rng"
      via: "rngOf(world)()"
      pattern: "rngOf\\(world\\)\\(\\)"
    - from: "sim.js decideAndAct (gossip honesty flip)"
      to: "world.rng"
      via: "rngOf(world)()"
      pattern: "rngOf\\(world\\)\\(\\)"
    - from: "sim.js pickScapegoat (weighted scapegoat pick)"
      to: "world.rng"
      via: "rngOf(world)()"
      pattern: "rngOf\\(world\\)\\(\\)"
    - from: "presentation.js"
      to: "Sim.seedRng"
      via: "called immediately after each Sim.createWorld()"
      pattern: "Sim\\.seedRng\\(world"
---

<objective>
Give the engine a single seeded RNG stream that lives on `world`, route every existing
`Math.random()` call site in `sim.js` through it, add the shared `Sim.TUNING` constants
block that Phases 2-7 will fill in, and add the `driftEnabled` read accessor the
regression harness needs.

Purpose: VERIF-02 (a session is reproducible from its seed) and VERIF-03 (one named
constants block) are prerequisites for the regression harness in Plan 02 — a
"byte-for-bit" reproduction check (VERIF-01) is impossible while three unseeded
`Math.random()` calls can change the outcome between runs.

Output: `sim.js` gains `mulberry32`, `DEFAULT_SEED`, `seedRng`, `rngOf`,
`isDriftEnabled`, and `TUNING` (all on the `Sim` export); the three RNG call sites are
rewired; `presentation.js` seeds each new world so live play is replayable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-infrastructure/01-CONTEXT.md
@.planning/phases/01-verification-infrastructure/01-PATTERNS.md
@CLAUDE.md

**Shell note:** every `<automated>` command below is written for **Git Bash**, not
PowerShell. Run them with the Bash tool from the repo root. `node -e` bodies use double
quotes on the outside and single quotes inside.

<interfaces>
<!-- Contracts this plan creates. Plan 02 and Plan 03 consume these directly. -->

New module-level constants in sim.js (top of file, alongside VALUES / WORLDVIEW_BELIEFS /
EMOTION_HALFLIFE_TICKS):

  const DEFAULT_SEED = <a fixed integer literal>;
  const TUNING = { /* Phase 2+ tuning numbers only — see D-06/D-07 */ };

New functions in sim.js:

  function mulberry32(seed)            -> () => number in [0, 1)
  function seedRng(world, seed = DEFAULT_SEED) -> world   (always re-seeds; no lazy guard)
  function rngOf(world)                -> () => number in [0, 1)   (installs default stream if absent)
  function isDriftEnabled(world)       -> boolean

New fields installed on `world` by seedRng:

  world.seed      : number   (the seed actually used — serializable, survives JSON.stringify)
  world.rngCalls  : number   (draw counter, starts 0, increments on every rng() call)
  world.rng       : function (the closure itself — deliberately NOT serializable)

Added to the `Sim` object literal at the bottom of sim.js:

  TUNING, DEFAULT_SEED, seedRng, isDriftEnabled
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Sim.TUNING constants block and the isDriftEnabled accessor</name>
  <files>sim.js</files>
  <read_first>
    - sim.js lines 1-45 (existing top-of-file constant block: LOCATIONS, VERBS, VALUES, WORLDVIEW_BELIEFS, PREDICATE_LABELS, EMOTION_HALFLIFE_TICKS)
    - sim.js lines 1027-1044 (clamp, the `Sim` object literal, and the dual-export guard)
    - .planning/phases/01-verification-infrastructure/01-CONTEXT.md — decisions D-02, D-06, D-07
    - .planning/phases/01-verification-infrastructure/01-PATTERNS.md §"`sim.js` — `Sim.TUNING` constants block" and §"`sim.js` — `world.driftEnabled` toggle"
  </read_first>
  <action>
Add a module-level `const TUNING = { ... };` to the top-of-file constant block in
`sim.js`, immediately after `EMOTION_HALFLIFE_TICKS` (line 42). Per D-07 it holds **new
Phase 2+ tuning numbers only** — it therefore ships this phase as an **empty object
literal with an explanatory comment above it** stating: what it is for (every new
tuning number introduced by Phases 2-7: thresholds, rates, decay constants), that it is
deliberately empty today because Phase 1 introduces no NPC-visible behavior, and that
pre-existing constants (`MAX_REACTION_DEPTH`, `EMOTION_HALFLIFE_TICKS`) are explicitly
NOT retrofitted into it. Do **not** invent placeholder numbers to populate it.

Add `function isDriftEnabled(world) { return world.driftEnabled !== false; }` alongside
the other small shared helpers (near `getAgent` / `coLocated` / `agentsAt`, around line
168-181).

Include a comment on `isDriftEnabled` recording *why* it exists in this exact shape,
because a future reader will otherwise be tempted to "fix" it:
  1. `createWorld()` is left literally untouched per D-02 — it does not gain a
     `driftEnabled` field, so an unseeded world reads `undefined`, which must mean
     "enabled".
  2. The `!== false` comparison is deliberate and load-bearing. `world.driftEnabled ||
     true` would silently coerce an explicit `false` back to enabled — the same failure
     class as the `params.quantity || 1` Known Bug documented in
     `.planning/codebase/CONCERNS.md`. Every future read of the flag (Phase 5's actual
     drift mechanic included) goes through this accessor, never through a raw truthiness
     test on the field.

Add `TUNING` and `isDriftEnabled` to the `Sim` object literal at the bottom of the file,
per CONVENTIONS.md's rule that new public capabilities are registered there rather than
exported ad hoc. Do not touch the `window`/`module` dual-export guard.

Do NOT modify `createWorld()` in this task or any other task in this plan.
  </action>
  <verify>
    <automated>node -e "const S=require('./sim.js'); if(typeof S.TUNING!=='object'||S.TUNING===null||Array.isArray(S.TUNING)) throw new Error('Sim.TUNING is not an object'); if(Object.keys(S.TUNING).length!==0) throw new Error('TUNING must ship empty this phase (D-07)'); if(typeof S.isDriftEnabled!=='function') throw new Error('Sim.isDriftEnabled missing'); const w=S.createWorld(); if(Object.prototype.hasOwnProperty.call(w,'driftEnabled')) throw new Error('createWorld() must stay untouched per D-02'); if(S.isDriftEnabled(w)!==true) throw new Error('unset must read as enabled'); w.driftEnabled=false; if(S.isDriftEnabled(w)!==false) throw new Error('explicit false must disable'); w.driftEnabled=true; if(S.isDriftEnabled(w)!==true) throw new Error('true must enable'); console.log('task1 ok')"</automated>
    <automated>COUNT=$(grep -c 'driftEnabled ||' sim.js || true); test "$COUNT" -eq 0 || { echo "falsy-coercion read of driftEnabled found"; exit 1; }; echo "no coercion ok"</automated>
  </verify>
  <acceptance_criteria>
    - `require('./sim.js').TUNING` is a plain object with exactly zero own keys.
    - `require('./sim.js').isDriftEnabled` is a function.
    - `Sim.createWorld()`'s returned object has no own `driftEnabled` property (D-02: createWorld untouched).
    - `isDriftEnabled(w)` returns `true` for unset, `false` for `w.driftEnabled = false`, `true` for `w.driftEnabled = true`.
    - `grep -n 'const TUNING' sim.js` reports a line number below the line matching `EMOTION_HALFLIFE_TICKS` and above the line matching `function makeAgent`.
    - `grep -c 'driftEnabled ||' sim.js` is 0 — no truthiness-coercing read of the flag anywhere.
    - Both `TUNING` and `isDriftEnabled` appear as keys in the `Sim` object literal at the bottom of sim.js.
    - `git diff sim.js` shows zero changed lines between `function createWorld() {` and its closing `}`.
  </acceptance_criteria>
  <done>`Sim.TUNING` exists as a documented empty block in the top-of-file constant group, `Sim.isDriftEnabled` exists with the `!== false` semantics and its rationale comment, both are exported, and `createWorld()` is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Add mulberry32, DEFAULT_SEED, seedRng, and rngOf</name>
  <files>sim.js</files>
  <read_first>
    - sim.js lines 1-45 (top-of-file constant block — where DEFAULT_SEED goes)
    - sim.js lines 44-90 (`makeAgent`'s trailing-opts pattern; `relOf`'s attach-state-onto-an-object pattern)
    - sim.js lines 166-186 (shared helpers section, and the `reactionDepth` module-level `let` that this must NOT imitate)
    - sim.js lines 1027-1044 (`Sim` object literal + dual-export guard)
    - .planning/phases/01-verification-infrastructure/01-PATTERNS.md §"`sim.js` — `Sim.seedRng(world, seed)` + `world.rng`" — contains the exact mulberry32 body to copy verbatim
    - .planning/phases/01-verification-infrastructure/01-CONTEXT.md — decisions D-02, D-03, D-04, D-05
  </read_first>
  <action>
Copy the `mulberry32(seed)` implementation **verbatim** from the fenced block in
`01-PATTERNS.md` §"Seeded PRNG algorithm — copy verbatim, do not reinvent" (it is the
algorithm already documented for this repo in `.planning/codebase/TESTING.md:31-38`).
Place it as a module-level function in `sim.js`. Do not substitute a different PRNG.

Add `const DEFAULT_SEED = <a fixed integer literal of your choosing>;` to the
top-of-file constant block. This is the always-reproducible seed the regression check
uses (D-04). It is **not** a Phase 2 tuning number — do NOT put it inside `TUNING`.

Add `function seedRng(world, seed = DEFAULT_SEED)` which:
  - sets `world.seed = seed` (a plain number, so it survives `JSON.stringify` into the
    Plan 02 snapshot and is visible in the debug report);
  - sets `world.rngCalls = 0`;
  - sets `world.rng` to a closure wrapping `mulberry32(seed)` that increments
    `world.rngCalls` on every draw and returns the value;
  - returns `world`.

Two non-negotiable properties of `seedRng`, both of which a careless implementation gets
wrong — call them out in comments:
  - It **always** (re)seeds. Do NOT copy `relOf`'s `if (!agent.mind.relationships[id])`
    lazy-init guard here; a second `seedRng(world, otherSeed)` call must genuinely
    re-seed, not silently no-op.
  - RNG state lives **on `world`**, never as a module-level `let`. `reactionDepth`
    (sim.js:185) is the one existing module-global and is flagged fragile in
    `.planning/codebase/CONCERNS.md:263-279` — do not add a second instance of that
    problem. This is D-03's explicit rationale.

Add `function rngOf(world)` which returns `world.rng`, lazily calling `seedRng(world)`
first if `world.rng` is absent. This is a defensive accessor so the engine can never
throw on a world that was built without an explicit `seedRng` call (e.g. an existing
`node -e` driver in `.claude/settings.local.json`'s allowlist). Comment that the
fallback uses `DEFAULT_SEED` and is a safety net, not the intended path — real entry
points seed explicitly.

Add a comment above `seedRng` recording the **LOCKED cross-phase RNG scope discipline**
(D-05), because Phases 5-7 will read this code: the only randomness permitted through
this stream is stochastic *texture* on the details of an already-decided action —
currently exactly three sites (Attack damage magnitude, gossip honest-vs-lying flip,
scapegoat selection). RNG must never decide *what* an NPC does; `decideAndAct()`'s
utility scoring is and must remain fully deterministic. Phase 6's snap threshold and
Phase 7's reactivation matching must be deterministic functions of context, never
probabilistic rolls.

Add `DEFAULT_SEED` and `seedRng` to the `Sim` object literal. `mulberry32` and `rngOf`
stay module-private (not exported) — nothing outside `sim.js` needs them.

Do NOT modify `createWorld()` (D-02).
  </action>
  <verify>
    <automated>node -e "const S=require('./sim.js'); const mk=()=>S.createWorld(); const draw=w=>[w.rng(),w.rng(),w.rng(),w.rng()]; const a=S.seedRng(mk(),42), b=S.seedRng(mk(),42), c=S.seedRng(mk(),43); const A=draw(a),B=draw(b),C=draw(c); if(JSON.stringify(A)!==JSON.stringify(B)) throw new Error('same seed diverged'); if(JSON.stringify(A)===JSON.stringify(C)) throw new Error('different seeds produced identical stream'); if(A.some(x=>!(typeof x==='number'&&x>=0&&x<1))) throw new Error('draw outside [0,1)'); if(a.rngCalls!==4) throw new Error('rngCalls not tracked, got '+a.rngCalls); if(a.seed!==42) throw new Error('world.seed not recorded'); const d=S.seedRng(mk()), e=S.seedRng(mk()); if(d.seed!==S.DEFAULT_SEED) throw new Error('default seed not applied'); if(d.rng()!==e.rng()) throw new Error('default seed not deterministic'); S.seedRng(a,42); if(a.rngCalls!==0) throw new Error('re-seed did not reset rngCalls'); if(a.rng()!==A[0]) throw new Error('re-seed did not reset the stream'); console.log('task2 ok')"</automated>
    <automated>node -e "const S=require('./sim.js'); const w=S.createWorld(); if(typeof w.rng!=='undefined') throw new Error('createWorld() must not install rng — D-02'); const s=JSON.parse(JSON.stringify(S.seedRng(w,7))); if(s.seed!==7) throw new Error('world.seed must survive JSON.stringify'); if(typeof s.rngCalls!=='number') throw new Error('world.rngCalls must survive JSON.stringify'); if('rng' in s) throw new Error('world.rng closure should be dropped by JSON.stringify'); console.log('serialization ok')"</automated>
    <automated>COUNT=$(grep -cE '^(let|var) ' sim.js || true); test "$COUNT" -eq 1 || { echo "expected exactly 1 module-level let (reactionDepth), found $COUNT — new module-global state is forbidden (D-03)"; exit 1; }; echo "no new module state ok"</automated>
  </verify>
  <acceptance_criteria>
    - `Sim.seedRng` and `Sim.DEFAULT_SEED` are exported; `mulberry32` and `rngOf` are not on the `Sim` object.
    - Two worlds seeded with the same explicit seed yield an identical first-4-draw sequence; seeds 42 and 43 yield different sequences.
    - Every draw satisfies `0 <= x < 1`.
    - `world.seed` and `world.rngCalls` are present, are numbers, and survive `JSON.parse(JSON.stringify(world))`; `world.rng` does not survive it.
    - `world.rngCalls` equals the exact number of `rng()` calls made since the last seeding.
    - Calling `seedRng(world, 42)` a second time resets both `rngCalls` to 0 and the stream to its first value (proves no lazy-init guard).
    - `Sim.createWorld()` alone installs neither `rng` nor `seed` nor `rngCalls`.
    - `grep -cE '^(let|var) ' sim.js` is exactly 1 (the pre-existing `reactionDepth`) — no new module-level mutable state.
    - The mulberry32 body in sim.js is character-identical to the block in 01-PATTERNS.md (whitespace/indentation aside).
    - A comment above `seedRng` states the D-05 scope rule naming `decideAndAct` as required-deterministic.
  </acceptance_criteria>
  <done>`Sim.seedRng(world[, seed])` attaches a reproducible mulberry32 stream plus `seed`/`rngCalls` bookkeeping onto `world`, re-seeds on every call, uses no module-level state, and `createWorld()` is still untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Route all three sim.js RNG call sites and both app entry points through the seeded stream</name>
  <files>sim.js, presentation.js</files>
  <read_first>
    - sim.js lines 254-296 (`applyEffects` — the Attack damage roll at line 278)
    - sim.js lines 930-965 (`decideAndAct` — the gossip honest-vs-lying flip at line 941)
    - sim.js lines 1007-1026 (`pickScapegoat` — the weighted roll at line 1019)
    - presentation.js lines 1-10 (file-top ownership comment and the module-level `let world = Sim.createWorld();`)
    - presentation.js lines 260-275 (`init()`'s reset-button handler, second `Sim.createWorld()` site)
    - .planning/phases/01-verification-infrastructure/01-PATTERNS.md §"`sim.js` — RNG call-site swaps"
  </read_first>
  <action>
Replace each of the three bare `Math.random()` calls in `sim.js` with `rngOf(world)()`.
All three enclosing functions already have `world` in scope — confirmed:
`applyEffects(world, actor, verb, params)`, `decideAndAct(world, witness, event,
appraisal, priorRelationship)`, and `pickScapegoat(world, witness, actualActorId,
victimId)`. No signature changes are needed anywhere.

  - `applyEffects`, `case 'Attack'` (line 278): `const damage = 15 + Math.floor(Math.random() * 15);`
  - `decideAndAct`, gossip block (line 941): `const truthful = Math.random() < clamp(0.5 + honestyWeight * 0.45, 0.05, 0.97);`
  - `pickScapegoat` (line 1019): `let roll = Math.random() * total;`

Preserve the surrounding arithmetic and the explanatory comments above each site exactly
— this is a mechanical substitution of the randomness source only, not a behavior
change. The damage range stays `15..29`, the truthfulness probability formula stays
identical, and the scapegoat weighting stays identical.

These three are the **complete** scope per the LOCKED D-05 rule. Do not seed any other
randomness. In particular, `parser.js:96` also calls `Math.random()` (picking a random
example command to show after a parse failure) — that is **explicitly out of scope**:
pure UI help-text flavor, not one of the three named sites, and `parser.js` is not in
this plan's `files_modified`. Leave it alone.

Then, in `presentation.js`, add `Sim.seedRng(world, Date.now());` immediately after each
of the two `Sim.createWorld()` calls — the module-level initializer near line 6 and the
reset-button handler inside `init()` near line 266. This is what makes VERIF-02's "a
session is reproducible from its seed" true of actual play: each session gets a
different stream, but `world.seed` records which one, so any session can be replayed by
passing that recorded seed to `Sim.seedRng`. This stays inside `presentation.js`'s
ownership boundary — it calls a public `Sim` API and does not mutate world state
directly.
  </action>
  <verify>
    <automated>COUNT=$(grep -c 'Math\.random' sim.js || true); test "$COUNT" -eq 0 || { echo "Math.random still present in sim.js ($COUNT occurrences)"; grep -n 'Math\.random' sim.js; exit 1; }; echo "sim.js clean"</automated>
    <automated>COUNT=$(grep -c 'rngOf(world)()' sim.js || true); test "$COUNT" -eq 3 || { echo "expected exactly 3 rngOf call sites, found $COUNT"; exit 1; }; PARSER=$(grep -c 'Math\.random' parser.js || true); test "$PARSER" -eq 1 || { echo "parser.js:96 Math.random must remain untouched (out of scope), found $PARSER"; exit 1; }; SEEDED=$(grep -c 'Sim\.seedRng(world' presentation.js || true); test "$SEEDED" -eq 2 || { echo "expected 2 Sim.seedRng calls in presentation.js, found $SEEDED"; exit 1; }; echo "call sites ok"</automated>
    <automated>node -e "const S=require('./sim.js'); const run=(seed)=>{const w=S.createWorld(); S.seedRng(w,seed); S.performAction(w,'player','Attack',{targetId:'mara'}); return {sig:JSON.stringify(w.events.map(e=>[e.verb,e.actor,e.data])), calls:w.rngCalls};}; const a=run(11), b=run(11), c=run(99); if(a.sig!==b.sig) throw new Error('same seed produced different event data'); if(a.calls<1) throw new Error('no rng draws recorded during a cascade — call sites are not wired'); if(a.sig===c.sig) console.log('NOTE: seeds 11 and 99 happened to coincide; not a failure'); console.log('task3 ok — rngCalls='+a.calls)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Math\.random' sim.js` is exactly 0.
    - `grep -c 'rngOf(world)()' sim.js` is exactly 3.
    - `grep -c 'Math\.random' parser.js` is exactly 1 — `parser.js:96` is untouched.
    - `grep -c 'Sim\.seedRng(world' presentation.js` is exactly 2, one immediately after each `Sim.createWorld()`.
    - Running `player Attack mara` twice from freshly-created worlds seeded with the same seed produces byte-identical `world.events` `[verb, actor, data]` payloads.
    - That run leaves `world.rngCalls >= 1`, proving draws actually flowed through the instrumented stream rather than an unseeded fallback.
    - `git diff sim.js` shows the three RNG lines changed with their surrounding arithmetic and comments intact — damage remains `15 + Math.floor(<draw> * 15)`, truthfulness remains `<draw> < clamp(0.5 + honestyWeight * 0.45, 0.05, 0.97)`, scapegoat remains `<draw> * total`.
    - No function signature in `sim.js` changed.
  </acceptance_criteria>
  <done>All randomness in `sim.js` flows through `world.rng`; the same seed reproduces the same event data across runs; the live app seeds itself from `Date.now()` and records that seed on `world.seed`; `parser.js` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player input → `parser.js` → `Sim.performAction` | Only untrusted-input boundary in the app. Unchanged by this plan; noted for completeness. |
| (no network, no filesystem, no auth) | Static single-player site, zero dependencies, no package-manager install tasks in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `world.seed` newly visible in the debug report and future snapshots | accept | The seed is a non-secret integer in a local, single-player, zero-auth static site with no PII. Exposing it is the point (VERIF-02: sessions replayable from their seed). |
| T-01-02 | Tampering | `mulberry32` replacing `Math.random()` | accept | Per D-05 the RNG is texture-only (Attack damage magnitude, gossip honesty flip, scapegoat pick). It is not and must never become a security primitive — no CSPRNG requirement applies. A comment on `seedRng` records this so a future phase does not repurpose it. |
| T-01-03 | Tampering | RNG scope creep in Phases 5-7 (a probabilistic roll deciding *what* an NPC does) | mitigate | The LOCKED D-05 rule is written as a comment directly above `seedRng` in `sim.js`, naming `decideAndAct` as required-deterministic and naming Phase 6 snap thresholds / Phase 7 reactivation matching as bound by it. |

*No `T-01-SC` supply-chain row: this is a zero-dependency project with no `package.json` and no package-manager install tasks in this plan.*
</threat_model>

<verification>
Run all task-level `<automated>` commands via the Bash tool from the repo root, then:

1. `node -e "const S=require('./sim.js'); console.log(Object.keys(S).join(','))"` — the
   export list must now include `TUNING`, `DEFAULT_SEED`, `seedRng`, `isDriftEnabled`
   alongside the pre-existing `LOCATIONS, VERBS, VALUES, WORLDVIEW_BELIEFS,
   PREDICATE_LABELS, createWorld, performAction, getAgent, memoryStrength`.
2. `git diff --stat` — exactly two files changed: `sim.js`, `presentation.js`.
3. Open `index.html` (or `python3 -m http.server 8000`) and confirm the app still loads
   and a typed command still works — this plan must not change any observable NPC
   behavior, only where its randomness comes from.
</verification>

<success_criteria>
- All three `<automated>` verification blocks pass.
- `grep -c 'Math\.random' sim.js` is 0; `grep -c 'Math\.random' parser.js` is 1.
- Same seed → byte-identical event data across separate process runs (VERIF-02).
- `Sim.TUNING` exists as a documented, deliberately-empty named constants block (VERIF-03).
- `Sim.createWorld()` is byte-identical to its pre-plan form (D-02).
- No new module-level mutable state in `sim.js` (D-03).
</success_criteria>

<output>
Create `.planning/phases/01-verification-infrastructure/01-01-SUMMARY.md` when done.
Record: the chosen `DEFAULT_SEED` value, the exact `Sim` export list after this plan, and
confirmation that `createWorld()` was not modified.
</output>
