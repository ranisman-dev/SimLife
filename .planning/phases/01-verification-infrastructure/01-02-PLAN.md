---
phase: 01-verification-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - sim.js
  - scripts/known-mismatch.json
autonomous: false
requirements: [VERIF-01]

must_haves:
  truths:
    - "A world snapshot contains exactly the agents actually involved in the scenario, derived from the event log — never a hardcoded agent count"
    - "Two identical snapshots produce zero diffs; a single changed nested field produces exactly one diff naming its dotted path, old value, and new value"
    - "A diff renders as human-readable field-by-field lines, not a raw JSON blob"
    - "A snapshot survives a real disk round trip: stringify to a file, read it back, parse it, and diff it against the live snapshot yields zero diffs"
    - "The two clones differ in exactly one field — the CompetitiveJungle worldview weight — and the check proves that programmatically; drift is proven off in both worlds. Neither of those two facts is ever escalatable."
    - "Running the two-clone CompetitiveJungle scenario either reproduces the divergent reaction documented in PERSON-MODEL.md (the CompetitiveJungle-positive clone attacks the player, the CompetitiveJungle-negative clone does nothing), or the mismatch reaches a blocking human checkpoint and is recorded in `scripts/known-mismatch.json` with `approved: true` — the assertion itself is never weakened to match whatever the code happens to do"
    - "The check returns a result object and prints nothing, preserving sim.js's zero-I/O boundary"
    - "D-08: Reproduction is checked via a full snapshot diff, not looser targeted assertions"
    - "D-09: The snapshot scope is derived from the agents actually involved, never a hardcoded agent count; diffs render field-by-field, not as a raw JSON blob"
  artifacts:
    - path: "sim.js"
      provides: "scenarioParticipants, snapshotWorld, diffSnapshots, formatDiff, runRegressionCheck"
      contains: "function runRegressionCheck"
  key_links:
    - from: "sim.js runRegressionCheck"
      to: "sim.js seedRng / isDriftEnabled"
      via: "seeds each scenario world with DEFAULT_SEED and sets world.driftEnabled = false"
      pattern: "driftEnabled = false"
    - from: "sim.js snapshotWorld"
      to: "sim.js scenarioParticipants"
      via: "default agent scoping when no explicit id list is passed"
      pattern: "scenarioParticipants\\(world\\)"
    - from: "Sim export object"
      to: "runRegressionCheck"
      via: "public API registration, enabling both the Node script and the browser console (D-01)"
      pattern: "runRegressionCheck"
---

<objective>
Build the regression harness inside `sim.js`: a scenario-scoped world snapshotter, a
snapshot differ with human-readable formatting, and `Sim.runRegressionCheck()` — which
reproduces PERSON-MODEL.md's two-clone `CompetitiveJungle` divergent-reaction case as a
repeatable, seeded, drift-off check.

Purpose: VERIF-01 requires reproducing that case "byte-for-bit" with drift off. D-08
specifies a full snapshot diff rather than looser targeted assertions. D-09.1 (LOCKED)
requires the snapshot to derive its agent set from the scenario rather than assuming a
fixed pair, because Phase 2's witness-ordering baseline will reuse this exact machinery
with three or more agents.

Output: five new functions in `sim.js`, all pure and I/O-free, registered on the `Sim`
export object so both `scripts/verify.js` (Plan 03) and the browser dev console reach
them with zero extra plumbing — plus, only if the documented claim genuinely does not
reproduce, a human-approved `scripts/known-mismatch.json` recording that finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-verification-infrastructure/01-CONTEXT.md
@.planning/phases/01-verification-infrastructure/01-PATTERNS.md
@.planning/phases/01-verification-infrastructure/01-01-SUMMARY.md
@CLAUDE.md

**Shell note:** every `<automated>` command below is written for **Git Bash**, not
PowerShell. Run them with the Bash tool from the repo root.

<interfaces>
<!-- Contracts this plan creates. Plan 03 consumes all of these. -->

  function scenarioParticipants(world) -> string[]      // sorted agent ids, derived
  function snapshotWorld(world, agentIds)               // agentIds optional
       -> { seed, rngCalls, tick, nextEventId, events, agents: { [id]: <plain agent> } }
  function diffSnapshots(before, after) -> Array<{ path: string, from: any, to: any }>
  function formatDiff(diffs) -> string                  // returns; never prints
  function runRegressionCheck(opts = {})
       -> { pass: boolean,
            checks: Array<{ name: string, pass: boolean, detail: string }>,
            snapshots: { [scenarioName: string]: <snapshot> },
            diffs?: Array<{ path, from, to }> }         // present only when opts.baseline given

  opts.baseline : a previously-captured `snapshots` object (Plan 03 loads it from disk)

Added to the `Sim` object literal:
  scenarioParticipants, snapshotWorld, diffSnapshots, formatDiff, runRegressionCheck

Already available from Plan 01 (do not redefine):
  DEFAULT_SEED, seedRng(world, seed), rngOf(world), isDriftEnabled(world), TUNING

<!-- Contract this plan may create (escalation branch of Task 3 only) -->

  scripts/known-mismatch.json
       { acknowledged: string[],      // check names that fail because the ENGINE
                                      // contradicts PERSON-MODEL.md's documented claim
         approved: boolean,           // false when Task 3 writes it; only Task 4's
                                      // human checkpoint may set it true
         approvedAt: string,          // ISO date, added by Task 4 on approval
         reason: string,              // substantive prose: what does not reproduce, and why
                                      // it is an engine/doc divergence rather than a bug here
         evidence: object,            // both clones' verbatim `chose` labels + failing checks
         personModelClaim: string,    // the PERSON-MODEL.md sentence being contradicted
         tuningAttempts: array }      // the W values / personalities tried before escalating

  Only `positive-clone-attacks-player`, `negative-clone-takes-no-action`, and
  `reactions-diverge` may ever appear in `acknowledged`. Plan 03's `scripts/verify.js`
  honors entries only when `approved === true`.
</interfaces>

<existing_shapes>
Relevant existing structures the executor must respect (read the cited lines, do not guess):

  world  = { tick, agents, events, nextEventId }                       sim.js:158-163
           plus seed / rngCalls / rng installed by seedRng (Plan 01)
  event  = { id, tick, verb, actor, location, data, causedBy, why }    sim.js:197-206
           `causedBy` is the eventId this action was a reaction to, or null
  agent  = { id, name, isPlayer, location, health, alive, inventory, mind }   sim.js:44-74
           `mind` is null for the player
  mind.reactedEventIds is a **Set** — plain JSON.stringify silently drops it
  mind.log entry = { tick, trigger, considered, chose, why }           sim.js:988-994
           trigger has the form `ev#<id> <verb> by <actor>`
  decideAndAct candidate labels: 'do nothing' (sim.js:876),
           `attack ${actorId}` (sim.js:907), `press ${actorId} for an explanation`,
           gossip label, 'retreat' (sim.js:978)
  An impact-too-small early return logs
           chose: "barely noticed — didn't care enough to react"      sim.js:861-871
           and performs no action at all. It fires when `appraisal.impact >= -0.05`,
           and pushes no log entry at all when `impact` is exactly 0.
  appraiseEvent (sim.js:472-495) has branches for only `Take` (unconsented), `Attack`,
           and `Give`. A `Tell` or `Move` event therefore yields `impact === 0` — this
           is MEMORY-01, an acknowledged Phase 4 gap, not a bug to fix here. Practical
           consequence for this plan: a witness who perceives a reaction-`Tell` takes no
           action and writes no log entry for it, so the victim's own gossip cascade does
           not make the clone act. Do not rely on that as a guarantee — scope the
           assertions by `causedBy` as specified below, which holds regardless.
</existing_shapes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scenarioParticipants and snapshotWorld</name>
  <files>sim.js</files>
  <read_first>
    - sim.js lines 117-181 (`createWorld`'s returned world shape; `getAgent`/`coLocated`/`agentsAt` helper style)
    - sim.js lines 44-74 (`makeAgent` — the exact agent/mind field set, including `mind: null` for the player and `reactedEventIds: new Set()`)
    - sim.js lines 197-213 (`performAction`'s event shape — `actor`, `data.targetId`, `causedBy`)
    - sim.js lines 296-300 (`computeWitnesses` — the closest existing "who was involved" concept, per-event not per-scenario)
    - presentation.js lines 130-140 (`serializeWorld` — the Set-aware JSON replacer this must reuse, adapted to take `world` as a parameter instead of closing over a global)
    - sim.js lines 1027-1044 (`Sim` object literal and the dual-export guard)
    - .planning/phases/01-verification-infrastructure/01-CONTEXT.md — decisions D-08, D-09
    - .planning/phases/01-verification-infrastructure/01-PATTERNS.md §"`sim.js` — `Sim.runRegressionCheck()`", Analog 2 and the "Snapshot scoping — D-09.1, LOCKED" note
  </read_first>
  <action>
Add `function scenarioParticipants(world)` returning a **sorted array of agent ids**,
derived — never hardcoded, never a fixed count. This is D-09.1, LOCKED, and Phase 2's
witness-ordering baseline reuses it verbatim with an attacker plus a victim plus multiple
bystanders. Include in the union:
  - every `event.actor` across `world.events`;
  - every `event.data.targetId` across `world.events` where that field is present;
  - every agent id whose `mind` is non-null AND has any of `mind.memories.length > 0`,
    `mind.reactedEventIds.size > 0`, `mind.log.length > 0`. This third clause is what
    catches a pure bystander who perceived the event but chose not to act — exactly the
    Phase 2 case — and is why the helper generalizes beyond the two clones.

Agents that neither acted, were acted upon, nor perceived anything are deliberately
excluded; CONTEXT.md D-09.1 explicitly permits leaving out state genuinely unrelated to
the tested interaction. Write a comment on the function saying so, and saying that the
one thing it must never do is assume how many agents a scenario involves.

Add `function snapshotWorld(world, agentIds)` returning a plain, fully
JSON-round-trippable object `{ seed, rngCalls, tick, nextEventId, events, agents }`,
where `agentIds` defaults to `scenarioParticipants(world)` when omitted, `events` is the
full `world.events` array, and `agents` is an object keyed by id containing only the
listed agents.

Serialization rules — get these exactly right, they are the whole correctness surface:
  - Reuse `presentation.js:134-137`'s replacer idiom: `JSON.parse(JSON.stringify(value,
    (key, v) => (v instanceof Set ? Array.from(v) : v)))`. Without it
    `mind.reactedEventIds` vanishes silently and a real change would diff as "no change".
  - `world.rng` is a closure and is dropped by `JSON.stringify`. That is **deliberate and
    correct** — the generator itself is not meaningful to diff — but it is only safe
    because `world.seed` and `world.rngCalls` (Plan 01) are plain numbers carried
    explicitly in the snapshot. `rngCalls` is what makes an RNG-consumption change
    diagnosable in one field, instead of appearing as a wall of unexplained numeric drift
    across every downstream value. Comment this so a later reader does not "fix" the
    omission.
  - Do not include an `rng` key in the returned object at all.

`snapshotWorld` must not mutate `world`. Register both functions on the `Sim` object
literal.
  </action>
  <verify>
    <automated>node -e "const S=require('./sim.js'); const w=S.createWorld(); S.seedRng(w,5); w.agents.elena.location='away'; w.agents.garrick.location='away'; S.performAction(w,'player','Attack',{targetId:'mara'}); const p=S.scenarioParticipants(w); if(!Array.isArray(p)) throw new Error('scenarioParticipants must return an array'); if(p.join()!==p.slice().sort().join()) throw new Error('result must be sorted'); if(!p.includes('player')||!p.includes('mara')) throw new Error('actor/target missing from participants: '+p); if(p.includes('elena')||p.includes('garrick')) throw new Error('uninvolved away-agents wrongly included: '+p); const s=S.snapshotWorld(w); if(Object.keys(s.agents).sort().join()!==p.slice().sort().join()) throw new Error('snapshot agent set != participants'); if('rng' in s) throw new Error('rng closure must not appear in the snapshot'); if(typeof s.seed!=='number'||typeof s.rngCalls!=='number') throw new Error('seed/rngCalls must be carried explicitly'); if(!Array.isArray(s.events)||s.events.length<1) throw new Error('events missing'); const flat=JSON.stringify(s); if(flat.indexOf('reactedEventIds')===-1) throw new Error('Set field dropped - the JSON replacer is missing'); if(JSON.stringify(JSON.parse(flat))!==flat) throw new Error('snapshot is not plain JSON-round-trippable'); const n=w.events.length; S.snapshotWorld(w); if(w.events.length!==n) throw new Error('snapshotWorld mutated the world'); console.log('task1 ok participants='+p.join(','))"</automated>
    <automated>node -e "const S=require('./sim.js'); const w=S.createWorld(); S.seedRng(w,5); const p0=S.scenarioParticipants(w); if(p0.length!==0) throw new Error('an untouched world must yield zero participants, got '+p0); const s=S.snapshotWorld(w,['mara','ives','tomas']); if(Object.keys(s.agents).sort().join()!=='ives,mara,tomas') throw new Error('explicit agentIds not honored: '+Object.keys(s.agents)); console.log('scoping ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `Sim.scenarioParticipants` and `Sim.snapshotWorld` are exported functions.
    - On a world where `player` attacked `mara` with `elena`/`garrick` relocated to `'away'`, `scenarioParticipants` returns a sorted array including `player` and `mara` and excluding `elena` and `garrick`.
    - On a freshly created world with no events, `scenarioParticipants` returns an empty array.
    - `snapshotWorld(world)` with no second argument produces `agents` keyed by exactly the `scenarioParticipants` set.
    - `snapshotWorld(world, ['mara','ives','tomas'])` honors the explicit list.
    - The snapshot has numeric `seed` and `rngCalls` top-level fields and has no `rng` key.
    - `JSON.stringify(snapshot)` contains the substring `reactedEventIds` — proving the `Set` replacer is applied.
    - `JSON.stringify(JSON.parse(JSON.stringify(snapshot)))` equals `JSON.stringify(snapshot)`.
    - Calling `snapshotWorld` does not change `world.events.length` or any agent field.
    - Neither function contains a numeric literal agent count nor a hardcoded list of agent ids (D-09.1).
  </acceptance_criteria>
  <done>`snapshotWorld` produces a plain, Set-safe, JSON-round-trippable snapshot scoped to a participant set that `scenarioParticipants` derives from the event log and per-agent perception state, with no assumption about how many agents are involved.</done>
</task>

<task type="auto">
  <name>Task 2: Add diffSnapshots and formatDiff</name>
  <files>sim.js</files>
  <read_first>
    - sim.js — the `scenarioParticipants`/`snapshotWorld` functions added in Task 1 (the diff operates on their output shape)
    - presentation.js lines 139-207 (`buildDebugReport` — the `lines.push(...)` / `lines.join('\n')` accumulator and terse `field=value` formatting idiom to extend)
    - sim.js lines 188-213 (`performAction` — the result-object-over-exceptions idiom these must follow)
    - .planning/phases/01-verification-infrastructure/01-CONTEXT.md — decisions D-08 and D-09.3
    - .planning/phases/01-verification-infrastructure/01-PATTERNS.md §"Result-object over exceptions/prints" and Analog 3
  </read_first>
  <action>
Add `function diffSnapshots(before, after)` returning an array of `{ path, from, to }`
objects, one per differing **leaf**:
  - `path` is a dot-joined key path from the snapshot root, e.g. `agents.mara.health`,
    `agents.ives.mind.relationships.player.grievance`, `events.2.data.damage`. Array
    indices appear as numeric path segments.
  - Recurse through plain objects and arrays; compare non-object leaves with `!==`.
    Treat `null` as a leaf.
  - A key present in `before` but absent in `after` yields `{ path, from: <value>, to:
    undefined }`; present in `after` but absent in `before` yields `{ path, from:
    undefined, to: <value> }`. Report an addition or removal at the highest path where
    the two structures first disagree — do not recurse into a subtree that exists on only
    one side.
  - Identical snapshots yield `[]`.
  - Deterministic ordering: walk keys in sorted order, so the same pair of snapshots
    always yields the same diff array.

Add `function formatDiff(diffs)` returning a **string** (D-09.3: human-readable,
field-by-field, old value to new value). Use the `lines.push(...)` / `lines.join('\n')`
accumulator idiom from `buildDebugReport`. Format each entry as `  <path>: <from> -> <to>`,
rendering `undefined` as the literal token `(absent)` and JSON-stringifying non-primitive
values. Use the **ASCII** `->` arrow, never the Unicode arrow, so the output survives
Windows terminal code pages. Return the exact string `no differences` when `diffs` is
empty.

Both functions are pure. `formatDiff` **returns** its string — it must never
`console.log`. `sim.js` has zero I/O anywhere today (no `console.log`, no file access);
that boundary is precisely what lets the same code back both the Node script and the
browser console (D-01). All printing belongs to the Plan 03 callers.

Register both on the `Sim` object literal.
  </action>
  <verify>
    <automated>node -e "const S=require('./sim.js'); const w=S.createWorld(); S.seedRng(w,5); S.performAction(w,'player','Attack',{targetId:'mara'}); const a=S.snapshotWorld(w); const b=JSON.parse(JSON.stringify(a)); if(S.diffSnapshots(a,b).length!==0) throw new Error('identical snapshots must yield zero diffs'); b.agents.mara.health=a.agents.mara.health-1; const d=S.diffSnapshots(a,b); if(d.length!==1) throw new Error('expected exactly 1 diff, got '+d.length+' -> '+JSON.stringify(d)); if(d[0].path!=='agents.mara.health') throw new Error('wrong path: '+d[0].path); if(d[0].from!==a.agents.mara.health) throw new Error('wrong from value'); if(d[0].to!==a.agents.mara.health-1) throw new Error('wrong to value'); if(JSON.stringify(S.diffSnapshots(a,b))!==JSON.stringify(d)) throw new Error('diff ordering is not deterministic'); console.log('diff ok')"</automated>
    <automated>node -e "const S=require('./sim.js'); const w=S.createWorld(); S.seedRng(w,5); S.performAction(w,'player','Attack',{targetId:'mara'}); const a=S.snapshotWorld(w); const c=JSON.parse(JSON.stringify(a)); delete c.agents.mara.health; const dr=S.diffSnapshots(a,c); if(dr.length!==1||dr[0].path!=='agents.mara.health'||dr[0].to!==undefined) throw new Error('removal not reported correctly: '+JSON.stringify(dr)); const e=JSON.parse(JSON.stringify(a)); e.agents.mara.newField=7; const da=S.diffSnapshots(a,e); if(da.length!==1||da[0].path!=='agents.mara.newField'||da[0].from!==undefined||da[0].to!==7) throw new Error('addition not reported correctly: '+JSON.stringify(da)); const f=JSON.parse(JSON.stringify(a)); f.rngCalls=a.rngCalls+1; const dn=S.diffSnapshots(a,f); if(dn.length!==1||dn[0].path!=='rngCalls') throw new Error('rngCalls drift not isolated to one field: '+JSON.stringify(dn)); console.log('add/remove/rngCalls ok')"</automated>
    <automated>node -e "const S=require('./sim.js'); const w=S.createWorld(); S.seedRng(w,5); S.performAction(w,'player','Attack',{targetId:'mara'}); const a=S.snapshotWorld(w); const b=JSON.parse(JSON.stringify(a)); b.agents.mara.health=1; const t=S.formatDiff(S.diffSnapshots(a,b)); if(typeof t!=='string') throw new Error('formatDiff must return a string'); if(t.indexOf('agents.mara.health')===-1) throw new Error('path missing from output'); if(t.indexOf('->')===-1) throw new Error('ASCII arrow missing'); if(/[^\x00-\x7F]/.test(t)) throw new Error('non-ASCII character in diff output'); if(S.formatDiff([])!=='no differences') throw new Error('empty-diff message wrong'); console.log(t)"</automated>
    <automated>COUNT=$(grep -c 'console\.' sim.js || true); test "$COUNT" -eq 0 || { echo "sim.js must remain I/O-free; found $COUNT console.* calls"; grep -n 'console\.' sim.js; exit 1; }; echo "zero-IO boundary intact"</automated>
  </verify>
  <acceptance_criteria>
    - `Sim.diffSnapshots` and `Sim.formatDiff` are exported functions.
    - Two structurally identical snapshots produce `[]`.
    - Changing exactly one nested numeric field produces exactly one entry with `path === 'agents.mara.health'` and correct `from`/`to`.
    - Deleting a key produces one entry with `to === undefined`; adding a key produces one entry with `from === undefined`.
    - Changing only `rngCalls` produces exactly one entry at path `rngCalls`.
    - Calling `diffSnapshots(a, b)` twice on the same inputs produces byte-identical arrays.
    - `formatDiff` returns a string containing the dotted path and the ASCII token `->`, with no non-ASCII characters.
    - `formatDiff([])` returns exactly `no differences`.
    - `grep -c 'console\.' sim.js` is 0 — neither function prints.
  </acceptance_criteria>
  <done>A pure differ reports every changed/added/removed leaf by dotted path with old and new values in deterministic order, and a pure formatter renders them as ASCII field-by-field lines, with `sim.js` still containing zero I/O calls.</done>
</task>

<task type="auto">
  <name>Task 3: Add runRegressionCheck reproducing the two-clone CompetitiveJungle case</name>
  <files>sim.js, scripts/known-mismatch.json (escalation branch only)</files>
  <read_first>
    - PERSON-MODEL.md lines 125-145 (the `CompetitiveJungle` hook description and the "Verified: two clones ... choose different reactions (`do nothing` vs. `attack player`)" claim this must reproduce)
    - sim.js lines 839-880 (`decideAndAct` head: `doNothingScore`, the do-nothing candidate at line 876, and the `impact >= -0.05` early return that logs `barely noticed`)
    - sim.js lines 884-912 (the confront/attack candidate — `CompetitiveJungle: getWorldviewWeight(witness,'CompetitiveJungle') * 0.2`, label `` `attack ${actorId}` ``)
    - sim.js lines 106-115 (`generalCareOf` — the other `CompetitiveJungle` hook, `- getWorldviewWeight(witness,'CompetitiveJungle') * 0.25`)
    - sim.js lines 985-999 (the `mind.log` push: `trigger`, `considered`, `chose`, and `if (best.action) best.action(why)`)
    - sim.js lines 117-164 (`createWorld`'s five hand-authored agents — the fixture factory per TESTING.md, frozen by D-02)
    - sim.js lines 422-470 (`perceiveEvent` — witness iteration order and the `reactionDepth`-guarded reaction)
    - .planning/phases/01-verification-infrastructure/01-CONTEXT.md — decisions D-01, D-04, D-08, D-09
    - .planning/phases/01-verification-infrastructure/01-PATTERNS.md §"Qualitative assertion vs. snapshot diff — both are needed" and §"Building the two clones — API constraint to flag"
  </read_first>
  <action>
Add `function runRegressionCheck(opts = {})` to `sim.js`.

**Scenario construction — two isolated worlds, one clone each.** Do NOT put both clones
in a single world. In a single world the victim reacts first (agent-list order puts
`mara` ahead of `ives` in `computeWitnesses`), her reaction cascade is witnessed by both
clones before either perceives the original event, and the qualitative assertion becomes
dependent on witness ordering — which Phase 2 (ORDER-01) is specifically going to
change. That would make Phase 1's own regression gate invert the moment Phase 2 lands.
Two isolated worlds running the identical script under the identical seed is the
controlled A/B that PERSON-MODEL.md:140-143 actually describes, and it is
ordering-independent by construction. D-09.1 is unaffected: each world's snapshot still
derives its participant set via `scenarioParticipants`, so the harness generalizes to
Phase 2's attacker/victim/multi-bystander case unchanged — the baseline is simply keyed
by scenario name.

Define a module-level `const CLONE_SPEC` holding the shared clone definition
(`personality`, `values`, the `CompetitiveJungle` weight magnitude, the clone agent id,
and the victim agent id). Build each variant like this:

  1. `const world = createWorld();`   — never modified (D-02); TESTING.md blesses it as
     the one legitimate fixture factory, so no new agent-construction export is needed.
  2. `world.driftEnabled = false;`    — VERIF-01's drift-off toggle. Read it back only
     through `isDriftEnabled`, never through a truthiness test.
  3. `seedRng(world);`                — no second argument, so it uses D-04's hardcoded
     `DEFAULT_SEED`, the always-reproducible one.
  4. Overwrite the clone agent's `mind.personality` (whole object), `mind.values` (deep
     copy) and `mind.worldview` (deep copy) from `CLONE_SPEC`. The two variants receive
     `worldview: [{ belief: 'CompetitiveJungle', weight: +W }]` and
     `[{ belief: 'CompetitiveJungle', weight: -W }]` respectively — **identical in every
     other respect**. Use `ives` as the clone and `mara` as the victim; both already
     exist in `createWorld()`.
  5. Relocate every other NPC to `'away'` — derived, not enumerated: every agent that is
     not the player, not the clone, and not the victim. This keeps the witness set
     minimal, since `computeWitnesses` returns agents at `'square'`.
  6. `performAction(world, 'player', 'Attack', { targetId: <victimId> })`, keeping the
     returned `event.id` — that is the event both clones are judged on.

Name the two scenarios `jungle` (positive weight) and `averse` (negative weight).

**Checks — return them, never print them.** Build a `checks` array of
`{ name, pass, detail }` entries; top-level `pass` is the AND of every entry. Include at
minimum these five, using exactly these names:

  - `clone-specs-differ-in-exactly-one-field` — programmatically prove the two variants'
    applied clone state differs **only** in the `CompetitiveJungle` weight: deep-compare
    the two clones' `mind.personality` and `mind.values`, and compare their
    `mind.worldview` with that one weight normalized out. This check is what stops a
    later tuning pass from quietly making the divergence trivially true by changing two
    things at once.
  - `positive-clone-attacks-player` — in the `jungle` world there is an event with
    `verb === 'Attack'`, `actor === <cloneId>`, `data.targetId === 'player'`, and
    `causedBy === <the player's Attack event id>`.
  - `negative-clone-takes-no-action` — in the `averse` world there is **no** event with
    `actor === <cloneId>` **and** `causedBy === <the player's Attack event id>`. Two
    things about this scoping are deliberate:
      - Assert on the absence of an *event*, not on a log label: the negative clone can
        legitimately land either on the `do nothing` candidate (sim.js:876) or on the
        `impact >= -0.05` early return that logs
        `barely noticed — didn't care enough to react` (sim.js:861-871), and both mean
        the same thing behaviorally.
      - Scope it by `causedBy`, exactly as `positive-clone-attacks-player` is scoped —
        do **not** assert "no event by the clone at all". The victim reacts first
        (`computeWitnesses` returns her ahead of the clone) and her reaction is itself an
        event the clone perceives *before* it ever perceives the player's Attack. Any
        action the clone takes in response to that intervening event has nothing to do
        with the variable under test, and an unscoped assertion would fail on it. Both
        clone assertions must key on the same `causedBy` value so the pair is symmetric.
  - `reactions-diverge` — the two clones' `mind.log` entry for that event (matched on
    `trigger` containing `ev#<id>`) have different `chose` values. Carry both `chose`
    strings verbatim in this check's `detail`, so a human reading the output sees the
    actual labels and a future regression shows *what* changed. A clone with no matching
    `mind.log` entry (per `<existing_shapes>`, the `impact >= -0.05` early return at
    `sim.js:858-864` pushes none) is not a check-implementation error — treat its `chose`
    value as the literal string `(no entry)` and compare normally; two `(no entry)` values
    do not diverge, `(no entry)` vs. any real label does.
  - `drift-disabled` — `isDriftEnabled(world) === false` in both worlds.

**Return shape.** `{ pass, checks, snapshots: { jungle, averse } }`, where each snapshot
comes from `snapshotWorld(world)` with no explicit agent list so `scenarioParticipants`
scopes it. When `opts.baseline` is supplied — an object of the same `{ jungle, averse }`
shape — additionally compute `diffs` by concatenating
`diffSnapshots(opts.baseline[name], snapshots[name])` for each scenario name, prefixing
each entry's `path` with `<name>.`, and fold "zero diffs" into `pass` as an extra check
named `snapshot-matches-baseline`. Without `opts.baseline`, omit the `diffs` key entirely
and do not fail on its absence — that is how the browser-console entry point (D-01) still
returns a meaningful result with no filesystem access.

`runRegressionCheck` must print nothing and must not throw on a failed check: a failure
is a `false` in the returned object, per the result-object idiom (CONVENTIONS.md:60-70).
It must never read the filesystem either — `scripts/known-mismatch.json` below is read
only by Plan 03's `scripts/verify.js`, never by `sim.js`.

**Tuning procedure, with hard guardrails.** Start with `W = 0.8` and a mid-range clone
personality (`boldness` 0.6, `neuroticism` 0.4, the remaining OCEAN traits 0.5) and
`values: [{ value: 'Justice', weight: 0.3 }]`. Run it. If the two variants do not diverge
into attack-player vs. no-action, adjust **only** `W` (toward ±1.0) and/or the shared
`CLONE_SPEC` personality — both variants always receiving the identical change — then
re-run.

If the failure is structural rather than a matter of degree — that is, the victim's own
reaction cascade is interfering rather than the clone simply scoring the wrong candidate
— one further structural lever is sanctioned: suppress the victim's reaction so the clone
is the sole reactor, by calling `world.agents[<victimId>].mind.reactedEventIds.add(1)`
before the attack. Event ids start at 1 (`createWorld()` returns `nextEventId: 1`) and
the player's Attack is the first action performed, so `1` is that event's id; state that
coupling in a comment so a later change to the scenario script does not silently
invalidate it. Apply it identically to both variants.

What you may **not** do under any circumstance is introduce a second differing field
between the two variants; the `clone-specs-differ-in-exactly-one-field` check exists to
catch exactly that and must keep passing.

**The two outcome branches — both have a defined, executable completion state.**

Two of the five checks are **never escalatable**, because the executor can always satisfy
them and a failure there is a defect in this task's own implementation rather than a
finding about the engine:
  - `clone-specs-differ-in-exactly-one-field` (the fixture is honest)
  - `drift-disabled` (drift is off in both worlds)
If either fails, fix the implementation. Do not escalate, do not write a mismatch file.

The other three are **behavioral claims about the engine**, and are exactly the claims
PERSON-MODEL.md makes: `positive-clone-attacks-player`, `negative-clone-takes-no-action`,
`reactions-diverge`.

  - **Branch A — the documented claim reproduces.** All five checks pass. Do not create
    `scripts/known-mismatch.json`; if an earlier tuning attempt left one behind, delete
    it. Task 4's checkpoint is a fast confirmation.

  - **Branch B — it still does not reproduce after tuning.** **Do not weaken, delete, or
    rescope any assertion to match whatever the code happens to do**, and do not
    introduce a second differing field. Instead, record the finding so it has an
    executable disposition:
      1. `mkdir -p scripts` (Plan 03 later adds `verify.js` and `baseline.json` to the
         same directory; creating it here is fine).
      2. Write `scripts/known-mismatch.json` with the shape given in `<interfaces>`:
         `acknowledged` listing exactly the failing check names — drawn **only** from the
         three behavioral checks above — `approved: false`, a substantive `reason`
         (at least a couple of sentences explaining what does not reproduce and why it is
         an engine-vs-doc divergence rather than a bug in this task), `evidence` carrying
         both clones' verbatim `chose` labels and the failing checks' `detail` strings,
         `personModelClaim` quoting the PERSON-MODEL.md sentence being contradicted, and
         `tuningAttempts` listing the `W` values and personalities tried.
      3. Write it with `JSON.stringify(obj, null, 2)` plus a trailing newline, so it is a
         line-oriented reviewable git diff like `baseline.json`.
      4. **Never write `approved: true` yourself.** Only Task 4, after a human approves,
         may set that. Plan 03's `verify.js` honors the file only when `approved === true`,
         so an unapproved file changes nothing about the gate.
    Then proceed to Task 4.

This is a real finding the phase exists to surface — CLAUDE.md's standing rule is that
drift between `PERSON-MODEL.md` and `sim.js` is a bug in one or the other — so it gets
recorded as data, not papered over and not left as an undefined "stop".

Register `runRegressionCheck` on the `Sim` object literal. D-01 names this exact symbol
and CONVENTIONS.md:99 requires new public capabilities to live on that literal — do not
create a separate namespace object or module file for it.
  </action>
  <verify>
    <automated>node -e "const S=require('./sim.js'); const fs=require('fs'); const r=S.runRegressionCheck(); if(typeof r!=='object'||r===null) throw new Error('must return an object'); if(typeof r.pass!=='boolean') throw new Error('missing boolean pass'); if(!Array.isArray(r.checks)||r.checks.length<5) throw new Error('expected at least 5 checks, got '+(r.checks||[]).length); if('diffs' in r) throw new Error('diffs must be omitted when no baseline is supplied'); const REQ=['clone-specs-differ-in-exactly-one-field','positive-clone-attacks-player','negative-clone-takes-no-action','reactions-diverge','drift-disabled']; const by={}; r.checks.forEach(function(c){ if(typeof c.name!=='string'||typeof c.pass!=='boolean'||typeof c.detail!=='string'||!c.detail) throw new Error('malformed check: '+JSON.stringify(c)); by[c.name]=c; }); REQ.forEach(function(n){ if(!(n in by)) throw new Error('missing check: '+n); }); if(Object.keys(r.snapshots).sort().join()!=='averse,jungle') throw new Error('expected snapshots keyed jungle/averse, got '+Object.keys(r.snapshots)); console.log(r.checks.map(function(c){return (c.pass?'PASS ':'FAIL ')+c.name+' :: '+c.detail;}).join('\n')); ['clone-specs-differ-in-exactly-one-field','drift-disabled'].forEach(function(n){ if(!by[n].pass) throw new Error('NOT escalatable - fix the implementation: '+n+' :: '+by[n].detail); }); const ESC=['positive-clone-attacks-player','negative-clone-takes-no-action','reactions-diverge']; const failing=ESC.filter(function(n){return !by[n].pass;}); const MM='scripts/known-mismatch.json'; if(failing.length===0){ if(!r.pass) throw new Error('every named check passes but r.pass is false'); if(fs.existsSync(MM)) throw new Error('the documented claim reproduces, so '+MM+' must not exist - delete it'); console.log('OUTCOME: reproduces'); } else { if(!fs.existsSync(MM)) throw new Error('branch B: '+failing.join(',')+' failed - write '+MM+' and take the Task 4 checkpoint. Do NOT weaken the assertion.'); const m=JSON.parse(fs.readFileSync(MM,'utf8')); if(!Array.isArray(m.acknowledged)||m.acknowledged.length===0) throw new Error('acknowledged must be a non-empty array'); m.acknowledged.forEach(function(n){ if(ESC.indexOf(n)===-1) throw new Error('only the three behavioral checks may be acknowledged, got: '+n); if(by[n].pass) throw new Error('acknowledged a passing check: '+n); }); failing.forEach(function(n){ if(m.acknowledged.indexOf(n)===-1) throw new Error('failing check not covered by '+MM+': '+n); }); if(typeof m.reason!=='string'||m.reason.length<80) throw new Error('reason must be substantive prose'); if(typeof m.personModelClaim!=='string'||!m.personModelClaim) throw new Error('personModelClaim missing'); if(!m.evidence||typeof m.evidence!=='object') throw new Error('evidence missing'); if(!Array.isArray(m.tuningAttempts)||m.tuningAttempts.length===0) throw new Error('tuningAttempts missing'); if(m.approved!==false) throw new Error('Task 3 must write approved:false - only the Task 4 human checkpoint may set it true'); console.log('OUTCOME: mismatch-escalated :: '+failing.join(',')); }"</automated>
    <automated>A=$(node -e "console.log(JSON.stringify(require('./sim.js').runRegressionCheck().snapshots))"); B=$(node -e "console.log(JSON.stringify(require('./sim.js').runRegressionCheck().snapshots))"); test "$A" = "$B" || { echo "runRegressionCheck is not reproducible across separate node processes"; exit 1; }; echo "cross-process reproducible"</automated>
    <automated>node -e "const S=require('./sim.js'); const fs=require('fs'); const os=require('os'); const path=require('path'); const live=S.runRegressionCheck().snapshots; const f=path.join(os.tmpdir(),'gsd-snap-roundtrip.json'); fs.writeFileSync(f, JSON.stringify(live,null,2)+'\n'); const back=JSON.parse(fs.readFileSync(f,'utf8')); fs.unlinkSync(f); Object.keys(live).forEach(function(n){ if(!(n in back)) throw new Error('scenario lost on the disk round trip: '+n); const d=S.diffSnapshots(back[n],live[n]); if(d.length!==0) throw new Error('snapshot did not survive stringify->disk->parse for '+n+' (a leaf is not JSON-safe, e.g. undefined/NaN inside event.why): '+S.formatDiff(d)); }); if(Object.keys(back).sort().join()!==Object.keys(live).sort().join()) throw new Error('scenario key set changed across the round trip'); console.log('disk round trip clean - baseline.json in Plan 03 will diff to []')"</automated>
    <automated>node -e "const S=require('./sim.js'); const base=S.runRegressionCheck().snapshots; const ok=S.runRegressionCheck({baseline:base}); if(!Array.isArray(ok.diffs)) throw new Error('diffs must be present when a baseline is supplied'); if(ok.diffs.length!==0) throw new Error('a fresh run must match its own baseline, got '+JSON.stringify(ok.diffs)); if(!ok.checks.some(function(c){return c.name==='snapshot-matches-baseline'&&c.pass;})) throw new Error('snapshot-matches-baseline check missing or failing'); const tampered=JSON.parse(JSON.stringify(base)); const firstAgent=Object.keys(tampered.jungle.agents)[0]; tampered.jungle.agents[firstAgent].health=-999; const bad=S.runRegressionCheck({baseline:tampered}); if(bad.pass!==false) throw new Error('a tampered baseline must fail the run'); if(bad.diffs.length<1) throw new Error('tampered baseline produced no diffs'); if(bad.diffs[0].path.indexOf('jungle.')!==0) throw new Error('diff paths must be prefixed with the scenario name, got '+bad.diffs[0].path); console.log(S.formatDiff(bad.diffs))"</automated>
    <automated>COUNT=$(grep -c 'console\.' sim.js || true); test "$COUNT" -eq 0 || { echo "sim.js must remain I/O-free"; exit 1; }; FS=$(grep -cE "require\('fs'\)|readFileSync|writeFileSync" sim.js || true); test "$FS" -eq 0 || { echo "sim.js must never touch the filesystem - known-mismatch.json is read only by scripts/verify.js"; exit 1; }; DRIFT=$(grep -c 'driftEnabled = false' sim.js || true); test "$DRIFT" -ge 1 || { echo "runRegressionCheck must set driftEnabled = false"; exit 1; }; echo "boundaries ok"</automated>
  </verify>
  <acceptance_criteria>
    - `Sim.runRegressionCheck` is exported and returns `{ pass, checks, snapshots }` with `snapshots` keyed exactly `jungle` and `averse`.
    - `checks` contains entries named `clone-specs-differ-in-exactly-one-field`, `positive-clone-attacks-player`, `negative-clone-takes-no-action`, `reactions-diverge`, and `drift-disabled`; each has a boolean `pass` and a non-empty string `detail`.
    - `clone-specs-differ-in-exactly-one-field` and `drift-disabled` both pass. These two are never escalatable — a failure in either is a defect in this task, and no `scripts/known-mismatch.json` may be written for them.
    - `positive-clone-attacks-player` and `negative-clone-takes-no-action` are both scoped by `causedBy === <the player's Attack event id>` — the negative check does NOT assert "no event by the clone at all", so an intervening reaction by the victim cannot fail it.
    - `reactions-diverge`'s `detail` contains both clones' verbatim `chose` labels.
    - Exactly one of the two outcome branches holds, and the branch is machine-checkable:
      - Branch A: all five checks pass, `pass` is `true`, and `scripts/known-mismatch.json` does **not** exist.
      - Branch B: one or more of `positive-clone-attacks-player` / `negative-clone-takes-no-action` / `reactions-diverge` fail, and `scripts/known-mismatch.json` exists with `approved: false`, an `acknowledged` array covering exactly those failing checks and containing nothing else, a `reason` of at least 80 characters, and non-empty `personModelClaim`, `evidence`, and `tuningAttempts`.
    - No assertion was weakened, deleted, or rescoped, and the two variants still differ in exactly one field, in either branch.
    - Called with no arguments, the result has no `diffs` key.
    - Called with `{ baseline: <its own snapshots> }`, `diffs` is `[]` and a `snapshot-matches-baseline` check is present and passing.
    - Called with a deliberately tampered baseline, `pass` is `false`, `diffs` is non-empty, and every diff `path` is prefixed with its scenario name (`jungle.` or `averse.`).
    - Two separate `node` processes produce byte-identical `JSON.stringify(snapshots)` — the check is reproducible from its seed, not just within one process.
    - The snapshots survive a real `stringify -> write file -> read -> parse` round trip: `diffSnapshots(parsed, live)` is `[]` for every scenario. This is the exact composition Plan 03's `baseline.json` depends on, proven in the plan that owns the snapshotter.
    - `grep -c 'console\.' sim.js` is 0, and `sim.js` contains no `require('fs')` / `readFileSync` / `writeFileSync`.
    - `createWorld()` is still unmodified (`git diff sim.js` shows no changed lines inside it).
  </acceptance_criteria>
  <done>`Sim.runRegressionCheck()` builds two isolated, identically-seeded, drift-off worlds whose clones differ in exactly one worldview weight, returns a structured pass/fail plus scenario-scoped snapshots that survive a disk round trip, optionally diffs against a supplied baseline, prints nothing — and lands in exactly one of two defined branches: the documented reaction pair reproduces, or the mismatch is recorded in an unapproved `scripts/known-mismatch.json` awaiting Task 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the reproduction outcome, or approve the recorded mismatch</name>
  <files>scripts/known-mismatch.json (touched only in Branch B, to set `approved: true`)</files>
  <read_first>
    - PERSON-MODEL.md lines 125-145 (the exact documented claim: two clones "choose different reactions (`do nothing` vs. `attack player`)")
    - scripts/known-mismatch.json, if Task 3's Branch B wrote one
    - CLAUDE.md (the standing rule that drift between PERSON-MODEL.md and sim.js is a bug in one or the other — which is why a mismatch is a finding worth recording, not a test to soften)
  </read_first>
  <action>
This checkpoint always runs; it is short in Branch A and substantive in Branch B. The
qualitative attack/no-action pair is the *only* tie-back this repo has to "the original
case" — per `01-PATTERNS.md` no pre-Phase-2 snapshot was ever stored — so a human eye on
that outcome is the one thing that cannot be automated away.

Before pausing, print for the human:
  1. `node -e "const S=require('./sim.js'); const r=S.runRegressionCheck(); console.log(r.checks.map(function(c){return (c.pass?'PASS ':'FAIL ')+c.name+' :: '+c.detail;}).join('\n')); console.log('pass='+r.pass);"`
     — the full check list, verbatim, including both clones' `chose` labels.
  2. In Branch B only: `cat scripts/known-mismatch.json`.

Then pause for the human. There are exactly two dispositions, and the phase continues
either way — the decision about what happens to `scripts/baseline.json` was already made
when this plan was written and is executed by Plan 03 Task 2, not re-litigated here:

  - **`approved`** (Branch A — every check passed): change nothing. Confirm no
    `scripts/known-mismatch.json` exists and continue.
  - **`accept-mismatch`** (Branch B — the human agrees this is genuine engine-vs-doc
    divergence): set `approved: true` in `scripts/known-mismatch.json` and add
    `approvedAt` as an ISO date string. Change nothing else in the file, and change
    nothing at all in `sim.js`. Plan 03's `verify.js` honors acknowledged entries only
    when `approved === true`, which is what lets it capture the baseline and exit 0 while
    still printing the failing check as `KNOWN-MISMATCH`, never as `PASS`.

If instead the human's response points at a defect in this task's implementation — a
mis-scoped assertion, a tuning mistake, the wrong clone or victim — that is the ordinary
fix-and-re-run path: fix Task 3, re-run its verification, and return here. It is not a
third disposition.

Never respond to this checkpoint by weakening, deleting, or rescoping a check, and never
add a second differing field between the two variants.

**Ordering note for the `<automated>` block below:** it is a *post-condition*, evaluated
after the human's resume-signal has been applied — not a pre-gate. In Branch B it asserts
`approved: true`, which is by definition false during the pause. Run it only after the
disposition has been recorded.
  </action>
  <what-built>
    Task 3 built `Sim.runRegressionCheck()`: two isolated, identically-seeded, drift-off
    worlds whose only difference is the sign of one `CompetitiveJungle` worldview weight.
    It asserts PERSON-MODEL.md's documented claim that the positive clone attacks the
    player while the negative clone does nothing. That claim was verified by hand once and
    never stored, so this run is the first time it has been machine-checked.
  </what-built>
  <how-to-verify>
    1. Read the printed check list. Confirm `clone-specs-differ-in-exactly-one-field` and
       `drift-disabled` both show `PASS` — if either shows `FAIL`, the fixture itself is
       wrong and this is a defect to fix, not something to approve.
    2. Read `reactions-diverge`'s detail and confirm the two `chose` labels are genuinely
       different and describe recognizable behavior (e.g. `attack player` vs.
       `do nothing` / `barely noticed — didn't care enough to react`).
    3. If `positive-clone-attacks-player`, `negative-clone-takes-no-action`, and
       `reactions-diverge` all show `PASS`: the documented claim reproduces. Reply
       `approved`.
    4. If any of those three shows `FAIL`: read `scripts/known-mismatch.json`. Confirm its
       `reason` describes a plausible engine-vs-doc divergence, its `tuningAttempts` show
       a real search was made, and its `personModelClaim` quotes the sentence being
       contradicted. If you accept recording this as a finding, reply `accept-mismatch` —
       the phase continues, `scripts/baseline.json` is still captured in Plan 03 (a
       behavioral golden master is still worth having, and every later phase needs
       something to diff against), and the mismatch is printed on every future run.
    5. If anything looks like an implementation mistake rather than an engine finding,
       describe it instead of choosing a disposition.
  </how-to-verify>
  <verify>
    <human-check>The human replies `approved` or `accept-mismatch`, or describes an implementation defect to fix.</human-check>
    <automated>node -e "const fs=require('fs'); const S=require('./sim.js'); const MM='scripts/known-mismatch.json'; const ESC=['positive-clone-attacks-player','negative-clone-takes-no-action','reactions-diverge']; const r=S.runRegressionCheck(); const by={}; r.checks.forEach(function(c){by[c.name]=c;}); const failing=ESC.filter(function(n){return !by[n].pass;}); if(failing.length===0){ if(fs.existsSync(MM)) throw new Error('all behavioral checks pass, so '+MM+' must not exist'); console.log('checkpoint outcome: approved (documented claim reproduces)'); } else { if(!fs.existsSync(MM)) throw new Error(MM+' missing for failing checks: '+failing.join(',')); const m=JSON.parse(fs.readFileSync(MM,'utf8')); if(m.approved!==true) throw new Error('the human has not approved yet - '+MM+' still has approved:'+m.approved); if(typeof m.approvedAt!=='string'||!m.approvedAt) throw new Error('approvedAt must be set when approved is true'); m.acknowledged.forEach(function(n){ if(ESC.indexOf(n)===-1) throw new Error('only behavioral checks may be acknowledged: '+n); }); failing.forEach(function(n){ if(m.acknowledged.indexOf(n)===-1) throw new Error('failing check not acknowledged: '+n); }); console.log('checkpoint outcome: accept-mismatch :: '+m.acknowledged.join(',')); }"</automated>
  </verify>
  <acceptance_criteria>
    - The human was shown the full verbatim check list (and, in Branch B, the contents of `scripts/known-mismatch.json`) before being asked to respond.
    - The human replied with exactly one of the two dispositions, or reported an implementation defect that was then fixed in Task 3 and re-verified.
    - Branch A: `scripts/known-mismatch.json` does not exist.
    - Branch B: `scripts/known-mismatch.json` has `approved: true`, a non-empty `approvedAt` ISO date, and an `acknowledged` array covering every failing behavioral check and containing nothing but behavioral check names.
    - No check logic in `sim.js` was changed in response to this checkpoint, and no second differing field was introduced between the two clone variants.
  </acceptance_criteria>
  <resume-signal>Reply `approved` (all behavioral checks passed), or `accept-mismatch` (record the PERSON-MODEL.md divergence and continue). If something looks like an implementation defect instead, describe it — that is a fix-and-re-run, not a disposition.</resume-signal>
  <done>The reproduction outcome has been seen by a human and has a recorded disposition: either the documented claim reproduces and no mismatch file exists, or the divergence is captured in an approved `scripts/known-mismatch.json` that Plan 03's verify script will honor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot serialization → diff → human review | The reviewer's only view of what changed. A silently-omitted field here is indistinguishable from "nothing changed", which is how a real regression gets blessed. |
| Task 3's result → Task 4's human checkpoint → `scripts/known-mismatch.json` | The only sanctioned path by which a failing documented-claim check can stop blocking the phase. Everything about that path is written down, human-approved, and committed. |
| (no network, no filesystem reads in `sim.js`) | All five engine functions are pure; `sim.js` performs no I/O. Filesystem access lives in Task 3's Branch-B write and, from Plan 03 onward, in `scripts/verify.js`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `snapshotWorld` silently dropping state (`Set` fields, closures) so a real change diffs as "no change" | mitigate | Reuse `presentation.js:134-137`'s `Set`-aware replacer; carry `seed` and `rngCalls` as explicit plain numbers; assert in Task 1's verification that `reactedEventIds` survives serialization, and in Task 3's that the snapshot survives a real `stringify -> disk -> parse` round trip with a zero-length diff. |
| T-01-05 | Tampering | An assertion weakened to match current behavior instead of PERSON-MODEL.md's documented claim | mitigate | The `clone-specs-differ-in-exactly-one-field` check is machine-enforced and never escalatable; the tuning procedure permits changing only shared fields; and a non-reproducing claim has exactly one sanctioned outlet — an unapproved `scripts/known-mismatch.json` that a human must approve at the Task 4 blocking checkpoint. Task 3's own verification fails if a behavioral check fails without that file, so silently softening an assertion is not a reachable state. |
| T-01-06 | Denial of Service | Unbounded reaction cascade inside `runRegressionCheck` | accept | Bounded by the pre-existing `MAX_REACTION_DEPTH = 4` guard in `perceiveEvent` (sim.js:461-469); this plan adds no new recursion. |
| T-01-07 | Repudiation | A regression whose cause is untraceable because every downstream value shifted at once | mitigate | `world.rngCalls` is carried in the snapshot, so a change in RNG consumption isolates to a single named diff field instead of appearing as unexplained numeric drift across the whole snapshot. |
| T-01-12 | Tampering | `scripts/known-mismatch.json` abused to silence a real regression rather than record a doc divergence | mitigate | Only the three behavioral check names may appear in `acknowledged` (machine-enforced in both Task 3's and Task 4's verification); `snapshot-matches-baseline` can never be acknowledged (enforced at load time in Plan 03 Task 1); Task 3 may only write `approved: false`, and Plan 03's `verify.js` ignores unapproved entries entirely; the file is a committed, pretty-printed fixture, so every change is a reviewable line-oriented git diff. |

*No supply-chain (`T-01-SC`) row: zero-dependency project, no `package.json`, no package-manager install tasks.*
</threat_model>

<verification>
Run every task-level `<automated>` command via the Bash tool from the repo root, then:

1. `node -e "console.log(Object.keys(require('./sim.js')).join(','))"` — the export list
   must now additionally include `scenarioParticipants`, `snapshotWorld`,
   `diffSnapshots`, `formatDiff`, `runRegressionCheck`.
2. `git status --porcelain` — the only changed paths are `sim.js` and, in Branch B only,
   `scripts/known-mismatch.json`.
3. `grep -c 'console\.' sim.js` returns 0 — the zero-I/O boundary that CLAUDE.md and
   CONVENTIONS.md both depend on is intact.
4. Open `index.html` and confirm the app still loads and a typed command still works —
   this plan adds functions only and must change no existing behavior.
</verification>

<success_criteria>
- All task-level automated checks pass.
- `clone-specs-differ-in-exactly-one-field` and `drift-disabled` pass — these are never escalatable.
- Exactly one branch holds, and it is machine-verifiable: either `Sim.runRegressionCheck()` returns `pass: true` with all five named checks passing and no `scripts/known-mismatch.json` on disk, or the failing behavioral check(s) are recorded in a human-approved `scripts/known-mismatch.json` (`approved: true`, `approvedAt` set) whose `acknowledged` list contains nothing but behavioral check names.
- No assertion was weakened, deleted, or rescoped, and the two clone variants still differ in exactly one field.
- The same check run in two separate `node` processes yields byte-identical snapshots (VERIF-01 reproducibility), and those snapshots survive a `stringify -> disk -> parse` round trip with a zero-length diff.
- Snapshot agent scoping is derived from the scenario, with no hardcoded agent count anywhere (D-09.1, reused by Phase 2).
- `sim.js` still contains zero `console.*` calls and no filesystem access, and `createWorld()` is still unmodified.
</success_criteria>

<output>
Create `.planning/phases/01-verification-infrastructure/01-02-SUMMARY.md` when done.
Record: the final `CLONE_SPEC` (personality, values, and the `W` value actually used),
both clones' verbatim `chose` labels, the participant set each scenario snapshot resolved
to, which outcome branch was taken, and the human's disposition at Task 4.

In Branch B, additionally record — under a heading a human will not miss — that
PERSON-MODEL.md's "do nothing vs. attack player" claim does not reproduce under current
engine behavior, which specific checks fail, the full contents of
`scripts/known-mismatch.json`, and the explicit consequence: `scripts/baseline.json` is
still captured in Plan 03 as a behavioral golden master, and every future
`node scripts/verify.js` run prints the acknowledged check as `KNOWN-MISMATCH` until
either the engine or `PERSON-MODEL.md` is changed to resolve the divergence.
</output>
